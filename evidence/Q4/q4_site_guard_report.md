# Q4-3 施工现场监理报告（q4_site_guard）
> **口径变更提示（2026-09-13，方案 A）**：本报告撰写于方案 A 之前；其中「表1『全天购电费』= 当日总费用」的结论已失效——四问该列现统一为 **本表购电量 × 该时段电价**（4 位小数），全年总购电费用（计划基础+调整相关+紧急）不落在表内任何一列。详见 \交付说明_Q3_v2.md\ / \交付说明_Q4-3.md\。

> 角色：reviewer（现场监理，只读）｜ 复核时点：2026-09-13 01:22–01:32 ｜ 完成判据：本报告落盘
> 复核方式：**不复用对话 8 的任何脚本**；价格取 `赛题\C题\附件\附件4.xlsx` 原始表，真值取 `8对话\output\q4_q3v2_seg.jsonl`，
> 负荷/光伏取 `附件2.xlsx` 原始表，模板取 `附件5\result4-3.xlsx`；全部自写探针只读运行，中间脚本落 `8对话\code\review_tmp\`。
> 期间未修改任何被审查文件。

---

## ① 一句话总判

**需返修（数字可信、交付件形态有一处硬伤）**：Q4-3 的优化结果与读法 C 三段式经独立复算**完全成立**（`J_cash = 13,727,033.657549`，与目标 13,727,033.66 只差 **0.002451 元**），逐段物理与 SOC 约束全通过；
但 `result4-3.xlsx` 的**「全天购电费」列整列按附件 1 价格生成**（Σ列 = 13,225,150.380835），与该问自身的附件 4 价格口径差 **501,883.28 元**，交付件无法复现自己的头条数字。7.6 侧证据经内容级核验**未见残留污染**，事故已恢复。

---

## ② §1.1 越界写入审计

### 2.1 指纹比对（`python site_guard_fingerprint.py --check`）

运行两次（复核起点 ≈01:22、终点 01:31:43），结果均为：

```
被改动 0 ｜ 被删除 0 ｜ 新增 0
```

监控面 275 个文件：`5对话`(146) + `7.5对话`(101) + `7.6对话`(10) + `赛题\C题\附件\附件5`(5) + `B对话\clean`(6) + `Q3交付`(7)，另含 5 个显式 `WATCH_FILES`（`7.5对话\code\q3_exec_v2b.py`、`7.6对话\code\q76_v2b_full.py`、`q76_materialize.py`、`5对话\code\q2_milp_plan.py`、`q2_v2_micro.py`）。

### 2.2 ⚠️ 指纹的适用范围（必须连带声明）

基线文件 `8对话\output\site_guard_fingerprint.json` 的生成时间是 **01:18:35**，而事故窗口是 **01:16:46–01:16:48**、恢复窗口是 **01:17:17–01:17:18**。
⇒ 本次 `--check = 0/0/0` 只能证明「**01:18:35 之后没有再次越界**」，**不能**用来证明「事故没发生过、或恢复是无损的」。下面 2.3/2.4 是我用事故前的原始记录另做的取证。

### 2.3 越界事实链（mtime 取证）

| 时点 | 事件 | 路径（是否在 `8对话\` 之外） |
| --- | --- | --- |
| 01:16:13–01:16:14 | 8 对话 `q4_q3v2_driver.py --run` 落逐段真值 | `8对话\output\q4_q3v2_seg.jsonl`（内） |
| 01:16:46 | 解法 JSON 落盘（8 对话 + Q4 交付镜像各一份） | `8对话\output\`、`Q4交付\04_证据\`（内 / 镜像） |
| 01:16:48 | `V.materialize(sp, sl, out)` → **`MAT_REPORT` 被写回 7.6 自己的目录**（事故本体，驱动脚本注释自述「01:16 曾因此覆盖一次，已恢复」） | `7.6对话\output\v2b_full\q76_v2b_full_materialize.json`（**外**） |
| 01:17:17 | 7.6 解法 JSON 被重写（恢复动作） | `7.6对话\output\v2b_full\q76_v2b_full_solution.json`（**外**） |
| 01:17:18 | 7.6 候选交付件与物化报告被重写（恢复动作） | `7.6对话\output\v2b_full\result3_v2_candidate.xlsx`、`…_materialize.json`（**外**） |
| 01:17:41 | `q4_q3v2_driver.py` 补上 `MAT_REPORT` 备份/还原保护 | `8对话\code\`、`Q4交付\05_代码\`（内 / 镜像） |
| 01:18:14 / 01:18:35 | 施工现场指纹脚本与其基线落盘 | `8对话\code\`、`8对话\output\`（内） |

**越界范围**：`D:\CMUCU\7.6对话\output\v2b_full\` 下 3 个文件（其中 `MAT_REPORT` 为事故时被覆盖、后被还原）。
**可能来源**：`7.6对话\code\q76_v2b_full.py:352` 的 `write_json(MAT_REPORT, rep)` —— 该写死路径在 `materialize()` 内部执行，调用方无法用参数改写；这是「借用他人函数时被他人写盘副作用牵连」的典型。01:17:17–18 的写回，形态与「用 7.6 自己的 `q76_jsonl_to_days.py` + `q76_materialize.py` 重新生成」一致（三者顺序落盘、上游 `…_seg.jsonl` 保持 00:44:56 未动），**判断为事后重新生成式恢复**（非字节还原）。

### 2.4 内容级核验：恢复是否无损（关键补充）

| 核验项 | 方法 | 实测 | 结论 |
| --- | --- | --- | --- |
| 7.6 逐段真值是否被 8 的数据污染 | 直接读 `q76_v2b_full_seg.jsonl`（00:44:56，事故前）与 8 的 `q4_q3v2_seg.jsonl` 同日比对 | 2025-02-01 段 0：7.6 `G⁰=1255.3637` vs 8 `G⁰=422.0304`；两边 `G⁰` 序列不同，`meta.source_seg` 各指自己 | **未污染** |
| 7.6 数字能否自洽复现 | 用 7.6 自己的 seg × 附件 1 价，独立实现读法 C | `J_plan=12,381,239.554313`、`J_adj=542,052.298589`、`J_emg=196,902.209236`、`J_cash=13,120,194.062139` | 与 7.6 事故前 `q76_v2b_full.json`(00:44:59) 记录、8 对话闸门期望 `13,120,194.06` **逐位一致** |
| 7.6 被重写的解法 JSON 是否忠实 | 7.6 seg ↔ 7.6 solution.json 逐值比对 | 334/334 天、`G0/A/C/D/H` 全等，差异 0 | **忠实** |
| 7.6 被重写的候选交付件是否忠实 | 当前 `result3_v2_candidate.xlsx` vs `Q3交付\01_提交件\result3_v2.xlsx`（后者字节 sha = 事故前 `RESULT_JSON` 记录的 `39f4c8dfc10aeac0…`，**逐字节未变**；注：方案 A 修订后该交付件字节 sha 为 `a9199e20d89060f8…`，修订仅涉第 147 列口径，见交付说明）逐格比对 | 四表全格差异 **0**（字节 sha 不同仅因 xlsx 容器写入时间） | **忠实** |
| 8 对话交付镜像是否同源 | `8对话\output\result4-3.xlsx` vs `Q4交付\01_提交件\result4-3.xlsx` | 字节 sha 相同 | 一致（注：此为方案 A 修订**前**的状态；方案 A 后交付件已更新，`8对话\output\` 保留的是修订前成品） |

**§1.1 结论**：当前（01:31:43）**0 个越界改动/删除/新增**；01:16–01:17 的越界写入已恢复，内容级核验未见残留污染。但恢复是「重新生成」而非「备份还原」，**没有字节级等价证明**（见 §1.3 - R2）。

---

## ③ §1.2 四项独立复核（实测值与绝对差）

### 3.1 输入盘点（先纠一处前提）

- `q4_q3v2_seg.jsonl`：**334 行**（2025-02-01 → 2025-12-31），每行 144 段，键 `d/date/G0/A/C/D/S/H/R_PV/R_G`；`G0/A/C/D/H` 为**段能量（kWh）**，故读法 C 三段式**不乘 dt**（乘 dt 只得 2,287,838.94，量级明显不符）。
- `q4_q3v2_solution.json`：顶层键只有 `meta / price / days`，**没有 `totals` 字段**（任务书设想的「与 solution.json 的 totals 对拍」在盘上不成立）。其 `days` 与 seg 逐值一致（334/334），`meta.price_csv = D:\CMUCU\B对话\clean\q1_clean.csv`（即附件 1 清洗件）。因此我改用「原始附件重算 → 与 xlsx 第 147 列、与 `q4_validator_result4-3.json` 三方对拍」。
- 复核期间主对话仍在产出（01:21:50 新写 `q4_validator_result4-3.json`、01:19:00 新写 `8对话\外部方案\Q4_3.2.md`），本报告数字对应 **01:22–01:32 的盘面快照**。

### 3.2 读法 C 三段式（附件 4 逐日 144 段价 × 逐段真值）

| 项 | 我独立重算 | 对话 8 侧记录 | 绝对差 |
| --- | ---: | ---: | ---: |
| `J_plan = Σ p₄·min(G⁰,A)` | **12,939,775.501638** | 12,939,775.501638（validator S6a） | 0.000000 |
| `J_adj = Σ[0.5p₄(G⁰−A)⁺ + 1.5p₄(A−G⁰)⁺]` | **579,594.274579** | 579,594.274579（同上） | 0.000000 |
| `J_emg = Σ 5p₄·H` | **207,663.881332** | 166,604.754132（同下，近似口径） | 41,059.127200 |
| **`J_cash`** | **13,727,033.657549** | 目标 **13,727,033.66** | **0.002451** ✅ |

**判定：通过**。`J_cash` 与目标仅差 2.45e-3 元（分位四舍五入量级），`J_plan/J_adj` 逐位相同。
`J_emg` 的 41,059.13 差是**工具口径差异**，不是数字错误：`q4_validator_result4-3.json` 的 S6a 只能读 xlsx（表 3 仅给「段合计」），紧急项被迫用**当日均价近似**，故其 `diff = −41,059.129651` 属**artifact**。精确值须用逐段真值（本报告 207,663.881332）。

### 3.3 逐段物理自检（全量 334 天 × 144 段，非抽样）

恒等式：`A + PV·dt + D + H = L·dt + C + R_PV + R_G`（`L/PV` 取自附件 2 原始表，`dt=1/6 h`）

| 指标 | 实测 | 判定 |
| --- | --- | --- |
| 最大逐段平衡残差 | **4.547e-13**（2025-02-25 段 70） | 通过（≤1e-6） |
| SOC 越界段数 / 区间 | **0** 段 ｜ `[1200.000000, 10800.000000]` | 通过 |
| 期末 SOC `S₁₄₄` | 7200.000093 kWh | 记录（附件 1 口径闸门运行为 7950.000093，属不同价格下的正常差异） |
| `χ = Σ min(C,D)` | **−1.637e-12**（≈0） | 通过 |
| 单段 `C/D ≤ 833.333` 越限段数 | **0 / 0** | 通过 |
| 附件 2 中 L/PV 缺失段数 | 0 | 数据完整 |

抽检 5 天（2025-02-01 / 05-15 / 07-20 / 10-01 / 12-31）最大残差分别为 `1.137e-13 / 2.274e-13 / 2.274e-13 / 2.274e-13 / 2.274e-13`，`χ` 全为 0，SOC 端点均在界内。

### 3.4 `result4-3.xlsx` 结构与口径

| 复核项 | 实测 | 判定 |
| --- | --- | --- |
| 四表名称与顺序 | `计划购电量 / 调整购电量 / 充放电量 / 紧急购电量`，与模板一致 | 通过 |
| 主表形状 | 335×147（= 模板；334 数据行） | 通过 |
| 表 3 展开 | 2005 行（2004 数据行 = 334 天 × 6 块） | 通过（模板仅 26 行骨架） |
| 表 4 展开 | 1542 行（1541 数据行 = 合并后段数，无 `⁝` 占位行） | 通过（模板仅 11 行骨架） |
| **位置映射**（第 2 列 = `[0:00,0:10)`） | 表 1 第 2 列 = 2025-02-01 段 0 = 422.0303666666667 = seg `G⁰[0]`；表 1 Σ144 列 = 表内「全天购电量」= Σ`G⁰`（334/334）；表 2 Σ144 列 = Σ`A`（334/334） | **通过**；与 Q3 交付件同款（`result3_v2.xlsx` 第 2 列 = 1255.3637 = 7.6 seg `G⁰[0]`） |
| 表 3 日期列每日只写一次 | 非空日期格 334 = 去重天数 334 | 通过 |
| 表 4 日期列每日只写一次 | 非空日期格 321 = 去重天数 321（321 天有紧急购电） | 通过 |
| **表 1「全天购电费」= 当日总费用（净退计划费 + 调整费 + 当日紧急费）** | 结构语义成立，但**整列按附件 1 价格生成**：与附件 4 口径逐日比 **334/334 天不符**，最大差 **11,440.577745**（2025-12-02）；`Σ列 = 13,225,150.380835` vs 附件 4 口径 `13,727,033.657549` | **不通过（绝对差 501,883.276714 元）** |

补充实测：把表 1 第 147 列换成**附件 1 价格**逐日重算，**334/334 天逐位吻合（最大差 0.000000）** ⇒ 该列确实整列走附件 1 价，来源即 `q76_v2b_full.py:73` 的 `PRICE_CSV = B对话\clean\q1_clean.csv`（附件 1 清洗件）经 `materialize(..., price_csv=PRICE_CSV)` 注入；表 2 第 147 列同样如此。

---

## ④ §1.3 风险提示（按严重度）

### 严重问题

| # | 问题 | 证据 | 修改建议 |
| --- | --- | --- | --- |
| S1 | **交付件「全天购电费」列价格口径混用（附件 1 vs 附件 4）**：Q4-3 的全部口径已换成附件 4 当日价，但表 1/表 2 的费用列仍按附件 1 常数价生成，交付件自身合计（13,225,150.38）复现不出头条数字（13,727,033.66），差 501,883.28 元（3.66%） | §3.4 实测；`q4_validator_result4-3.json` 的 `S2b`(334/334 失配)、`S6b`(296/334 超 1% 容差) 同为该因 | 重物化：物化引擎需支持**逐日 144 段价格矩阵**（现签名只吃单个 144 向量 `price_csv`），把 `p₄[d]` 传入并重跑 `--mat`，然后**加硬闸门**：`Σ表1第147列 ≡ 13,727,033.66`（同价格口径），不等则阻断交付 |
| S2 | **交付索引与交付件不自洽**（S1 的下游影响）：`Q4交付\00_README_交付索引.md:19` 已写死「**Q4-3 = 13,727,033.66**」，但该数字目前只能由逐段真值复算得到，交付件列合计给不出；且同一 README 第 18 行把 Q3 v2 写作 13,120,194.06（正确），两问费用列不可跨问比较的声明也需同步落到 Q4-3 | 同上 + §3.2 | S1 修完后重跑交付索引与附录；在交付说明中显式写「第 147 列的全年合计 = 该问头条 `J_cash`，若两者不等即为口径混用」的自检语句 |

### 需要改进

| # | 问题 | 证据 | 修改建议 |
| --- | --- | --- | --- |
| R1 | **施工现场指纹的基线晚于事故**，`--check=0` 容易被误读为「从未越界」 | 基线 01:18:35 vs 事故 01:16:48 | 把基线纳入交付物并在**每次跨目录写操作前后各跑一次**；对「借用他人脚本」的场景，先在调用前 `--check`，调用后再 `--check`，把两次输出一并留档 |
| R2 | **恢复方式无字节级等价证明**：01:17:17–18 为重新生成式恢复，虽经逐格核验一致，但缺少「备份 → 还原 → 哈希对齐」的证据链 | §2.3 / §2.4 | 今后一律先复制备份、恢复后比对 sha；若确需重新生成，须在恢复记录里写明「生成命令 + 前后逐格比对结果」 |
| R3 | **`solution.json` 缺 `totals` 与末态字段**，导致「与 totals 对拍」这一验收动作无处落地，只能绕道 xlsx 与验证器 | §3.1：顶层只有 `meta/price/days` | 在 `meta` 内补 `totals{J_plan,J_adj,J_emg,J_cash}`、`S_end`、`price_regime:"attachment4_daily"`，让交付件与真值文件自证口径 |
| R4 | **S6a 属近似检验，易产生误报**：验证器只能读 xlsx，紧急项用当日均价折算，得到 166,604.75（精确 207,663.88），差值 −41,059.13 会被当成「对拍失败」 | §3.2 | 把 S6a 改为直接读 `q4_q3v2_seg.jsonl` 逐段真值；xlsx 侧只保留「列合计 = 14X 列一致性」的结构检验 |
| R5 | **S2b 检验式与表 1 口径不符**：它假设「全天购电费 = Σp·G」，而冻结口径是「净退计划费 + 调整费 + 当日紧急费」，故它必然 334/334 fail，形成噪声并可能掩盖真问题 | `q4_validator_result4-3.json` errors 第 1 条；§3.4 | 改成 `= J_plan + J_adj + J_emg`（同价格口径），并把「价格口径必须与文件的价格区制一致」写成显式断言 |
| R6 | **表头字面 vs 位置映射的歧义**：模板第 2 列表头原文是 `0:10-0:20`、第 145 列是 `0:00-0:10+1`，而数据按「第 2 列 = `[0:00,0:10)`」放置 | §3.4 + 模板/Q3 交付件同款 | 属继承口径（Q3 同款），不是 Q4-3 新增风险；但**必须在交付说明里显式声明**「表头行照抄模板、数据按位置映射写入」，否则评阅方按表头逐字读会判成整体错位一列 |
| R7 | **7.6 三件产物的 mtime 落在事故窗口**：内容虽已核验无损，但任何按「mtime 未变」推断完整性的自动化检查都会在此误报 | §2.3 | 在 7.6 侧留一页「恢复记录」说明 01:17 的重写原因与核验结论，避免后续对话把它当成新的污染事件 |

### 优化建议

| # | 建议 | 目的 |
| --- | --- | --- |
| O1 | 把 `materialize()` 的写路径副作用做成**断言**：调用前用 `os.path.abspath` 白名单校验「所有输出必须在 `8对话\` 内」，越界即抛异常（现在只备份了 `MAT_REPORT` 一个文件，guard 面靠人工枚举） | 从「事后备份还原」升级为「事前拒绝」 |
| O2 | 把指纹监控面补上 `Q4交付\` 与 `8对话\output\` 的关键产物（现仅覆盖 7.6/7.5/5/B/Q3 与赛题附件） | 交付镜像与本地成品同步性也能被自动盯住 |
| O3 | 在 `q4_q3v2_driver.py --run` 落盘后立即打印一行「`J_cash`、`Σ表1第147列`、Δ」，Δ≠0 直接退非零码 | 一行自检即可拦住 S1 这类口径混用 |
| O4 | 性能上 `site_guard_fingerprint.py` 每次对 ≤512 KB 的文件做 sha256、大文件只记 size+mtime，275 个文件约 1 秒，开销可接受；若监控面继续扩大，可为「大文件」补上分块抽样哈希 | 防越界能力与开销的平衡 |

---

## 附：本报告用到的自写探针（只读，落 `8对话\code\review_tmp\`）

`rv_probe.py`（schema 探针）、`rv_struct.py`、`rv_att4.py`/`rv_att2.py`（附件结构）、`rv_recalc.py`（三段式独立重算）、
`rv_verify76.py`（7.6 证据自洽）、`rv_xlsx.py`/`rv_xlsx2.py`/`rv_xlsx3.py`（xlsx 结构与费用列口径）、`rv_hdr.py`（表头）、
`rv_phys.py`（全量物理自检）、`rv_q3x.py`（跨问映射一致性）、`rv_sha76.py`（事故前哈希比对）、`rv_last.py`（逐格比对）、
`rv_tables.py`/`rv_tpl3.py`（表 2/3/4 口径）。全部为只读脚本，未调用对话 8 与 7.6 的任何业务代码。
