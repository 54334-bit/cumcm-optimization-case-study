# CUMCM 删除文件恢复台账

## 基线

- 恢复方法：Codex 历史会话 JSONL 只读取证。
- 证据目录：`<CODEX_HOME>/sessions/2026/09`（仅本机使用，不提交其中内容）。
- 当前保护分支：`main`，恢复文件验证前不覆盖现存文件。
- 写入原则：控制器单写；子任务仅返回证据。
- 状态：`exact`、`reconstructed`、`partial`、`reference-only`、`unrecoverable-binary`。

## 执行状态

| 任务 | 角色 | 范围 | 隔离 | 状态 | 验证 | 发现 | 集成 |
|---|---|---|---|---|---|---|---|
| A | explorer | Q1–Q4、q1-q3重构、灵敏度分析 | 只读会话 | blocked | 控制器接管 | 子任务未返回；全会话索引已由控制器完成 | 已接管 |
| B | explorer | 论文优化、TeX、附录 | 只读会话 | passed | 证据由控制器复核 | 找到两版最终 TeX 恢复链和结构锚点 | 已集成 |
| C | explorer | 原目录树、代码、CSV/JSON、二进制清单 | 只读会话 | blocked | 控制器接管 | 子任务额度中断；9 月 16 日审计与复制映射已由控制器提取 | 已接管 |
| Controller | writer | 清单、恢复器、暂存恢复、Git/PR | 单写 | passed | 测试、历史哈希、TeX 结构与路径扫描通过 | 已恢复两份汇总方案、最终 TeX、两份台账、附录 TeX 和七份代码 | 已集成，待 PR |

## 裁定

- Ruling: 只有完整内容或完整基线加无歧义补丁链才能回填原路径；片段不得拼接成“原件”。
- Ruling: PDF、XLSX、PNG、数据库和索引若无二进制载荷，只登记路径、大小、哈希和可重建条件。
- Ruling: 当前公开仓库内容均为受保护用户成果，恢复过程不删除、不重命名、不覆盖。
- Ruling: 命中历史哈希的汇总方案原件保存在 Git 忽略的隔离暂存区；公开副本仅脱敏五处个人绝对路径。
