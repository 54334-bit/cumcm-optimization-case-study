# Q2 交付说明（主口径：非预见 + 因果保守裕度）

> 提交件：`D:\CMUCU\5对话\result2.xlsx`（= `result2_对话5_因果裕度非预见.xlsx`，由官方模板另存）
> 日期：2026-09-12 ｜ 本文件为 **Q2 唯一现行口径基准**（此前"确定性 A 口径"版本已作废为"完全信息下界"）

## 0. 口径声明（可写入论文）

> 问题二在每天 0:00 制定当天计划时，**当天真实负荷与光伏尚未发生**：负荷按当日已知的负荷曲线（附件2 给出的负荷序列；题面仅对光伏给出预报/预测，未给负荷预报）——该集合下文立为**假设 A1（负荷当日可得）**，光伏采用**可得信息的预报**——本文取**过去 4 天实际光伏曲线的均值**作为日前点预报，并按**因果保守裕度**（用 d 之前 28 天的历史**相对预报误差分布**的 20% 分位数，逐日重估，均值 ≈7%）下移，以对冲 5 倍紧急购电的代价不对称；**结算**用附件2 当天**实际**负荷与实际光伏，缺口按交易时刻电价的 5 倍紧急购电。紧急购电由预报误差驱动，**不应先验设为 0**。完全信息口径（计划直接用当天实际光伏）仅作理想下界。

依据：题面 Q1 明确"给出光伏发电功率一天的**预测数据**"，Q2 只说"根据附件 1 中的电价和附件 2 中的数据"，Q3 才引入 0/6/12/18 时**预报**；附录2 把附件1 光伏列称为"**预测**功率"、附件2 称为"实际功率"。实测确认附件1 三列 = **全年均值剖面**（与附件2/附件4 的逐时段年均值：负载 MAE 0.000028 kW、光伏 MAE 0.000905 kW、相关 ≈1）。

## 1. 冻结口径

| 项 | 内容 |
| --- | --- |
| 计划（0:00） | 负荷 = 当日实际负荷（**假设 A1：题面未给负荷预报，按当日可得处理**）；光伏 = 过去 4 天实际光伏均值 × (1 − 因果裕度) |
| 因果裕度 | 逐日重估：`margin_d = clip(−Q20(过去28天相对预报误差), 0, 0.35)`，实测均值 ≈ 7%；分位数 20% 由报童临界比 `4/(4+1)=0.8` 推出 |
| 结算（执行） | 用附件2 **实际**负荷与**实际**光伏；缺口按当时电价 5 倍紧急购电；富余先充电→弃光伏（**不允许售电/回网**，购电量非负） |
| 储能 | 容量 [1200, 10800] kWh；功率 ≤ 5000 kW（即 ≤ 833.33 kWh/10 分钟）；η = 0.9；2025-01-01 0:00 = 6000 kWh；SOC 链式传递 |
| 范围 | 334 天（2025-02-01~12-31；**依据：题面附件5 规定输出区间为 2025.2.1–12.31，模板表1 实测 335 行 = 表头 + 334 天**）；144 段/日；电价为附件1 单日曲线（Q2 每天相同） |

## 2. 关键数字

| 量 | 值 |
| --- | ---: |
| 计划购电费 `J_plan` | **12,891,818.24 元** |
| 紧急购电费 `J_emg`（5 倍价） | **360,522.85 元** |
| **全年购电费用** | **13,252,341.09 元** |
| 计划购电量 `QG` | 21,283,432.62 kWh |
| 充电量 / 放电量 | 6,684,044.85 / 5,412,321.33 kWh |
| **紧急购电** | **242 天 / 375 段 / 55,801.05 kWh**；单段最大 5,480.4 kWh |
| 储能日界 SOC | ∈ [6000, 8085.49]；首日 0:00 = 6000 kWh，**年末 24:00 = 7,950.0 kWh**（λ=0.4720 终端残值口径，**非**硬性回 6000） |

**论文表3（题面点名的 4 个日期，实测）**

| 日期 | 紧急购电时段 | 电量（kWh） |
| --- | --- | ---: |
| 2025-03-20 | 20:30-20:40 | 157.19 |
| 2025-06-21 | 无 | 0 |
| 2025-09-23 | 20:30-20:40 | 26.07 |
| 2025-12-21 | 无 | 0 |

## 3. 表结构约定

- **表1 列映射（位置映射）**：第 2 列 = 当日首个时段 `[0:00,0:10)`，第 145 列 = `[23:50,24:00)`。附件中的 `0:10`、`0:20` 等标签是相应时段的**右端点**；模板表头相对列位置整体后移一格并回卷（**判别性实测**：同一份文件，按**位置**读并贪心执行 → 与本文一致的 375 段/55,801.05 kWh；按"标签对齐"读 → 全年 257,989.97 kWh、**1,819 个「天×时段」缺口计数（非 1,819 天）** → 只有位置映射物理可行）。
- **表2**：每天 6 个四小时块填执行层实际充/放电量；每块首行"时刻/储电量"= `0:00` 与当日 0:00 SOC，次行 = `24:00` 与当日 24:00 SOC。
- **表3**：**日期列只在当日首段写一次，同日其余行留空（空白 = 沿用上一行日期，与模板表4 示例一致）**；375 段分布在 **242 天**（其余 92 天无）——粗略的“逐行数日期”解析会得到 242（不是 375、也不是 133）；**只写真实发生紧急购电的时段**（同日相邻正区间合并、跨日不合并），全年共 **375 行**（表头 + 375 段），与模板示例格式一致；无紧急购电的日期不占行、不填 0 行。

## 4. 验证链

| 环节 | 结果 |
| --- | --- |
| 数据层 | 附件1 电价 144 值、附件2 全年 365×144 负载/光伏与模型输入**逐元素相等（max diff 0.0）** |
| **信任层 L3（仅对 Q1）** | 我自写 LP 从官方附件独立解 Q1 → **35,126.95 元 / 59,482.70 kWh**，与两个**外部独立来源**（公开实现、易错点手册）逐位一致 ⇒ **数据链与求解器获外部交叉验证**（该验证针对 Q1；**Q2 主链未获外部独立复现**，仅内部核验 + 子代理独立复查） |
| CP5（本口径，内部） | **ALL PASS**：从成品 xlsx 反解 `J_plan 12,891,818.24`（逐行偏差 5e-5）、表3 375 段/55,801.046 kWh、贪心执行复现紧急 375 段/55,801.046 kWh、费用 13,252,341.09、SOC 链偏差 5.4e-6、SOC∈[6000,8085.5] |
| **子代理独立复查**（外部第二视角） | `output\review_R24_独立复查_数值与物化.md`：换实现路径重算主链（逐项/多数逐位一致）；并证实**因果性：不使用未来信息**（改 `P[d]` 不影响计划） |
| 可复现命令 | `python code\q2_emg_detail.py`（求解+执行）→ `q2_s5_materialize_causal.py`（物化）→ `q2_s6_verify_causal.py`（核验）；信任层 `q2_trust_layer.py` |

6. **终端残值 λ 的敏感性（新增）**：在现行口径下重扫 8 个 λ 点（0 / 0.15 / 0.30 / 0.40 / **0.4720** / 0.55 / 0.70 / 0.90），全年费用呈**三级台阶**：λ≲0.47 → 年末放空至 1,200（13,251,225.13 元）；**λ=0.4720 → 年末 7,950（13,252,341.09 元）**；λ≳0.55 → 年末充满至 10,800（13,287,931.82 元）。**极差仅 36,706.69 元（0.277%）**，即费用对终端口径**不敏感**；本文取值比最便宜的放空解只贵 **1,115.96 元（0.008%）**，却避免“年末耗尽储能换收益”的边界效应。数据 `output\q2_lam_new_caliber.json`，脚本 `code\q2_lam_scan_newcaliber.py`。
7. **假设 A1（负荷当日可得）与其灵敏度**：题面**仅对光伏**给出预报/预测，未给负荷预报；本文按题面把**当日负荷曲线视为计划阶段可得**（**假设 A1**）。这**不**是“使用未来信息”的一般主张：本模型对**光伏**严格遵守非预见（预报只用 d 之前信息，已由子代理独立证实）。**若进一步假设负荷也不可知**（负荷取过去 4 天情景），全年费用升至 **16,110,845.24 元**（+21.6%；不加保守裕度 16,336,148.08）。证据 `output\q2_load_unknown_arm.json`。


**λ=0.4720 的来源与限度（必须披露）**：该值沿自**早期确定性口径**的日界链对偶标定（边际量 C'(6000)=0.4711，落在 [0.4424, 0.4802] 内）；**在现行非预见口径下未重新标定**，只做了 8 点敏感性。因实测其为**弱参数**（三级台阶、极差 36,706.69 元 = 0.277%），故论文中应写作“**建模选择 + 敏感性已验证**”，**不得**写成“最优 λ”。

**术语澄清（避量纲误读）**：“**区间**” = 10 分钟时段（144/日）；“**段**” = **连续合并时段**（同日相邻正紧急购电区间合并，跨日不合并）。因此“单段最大 **5,480.44 kWh**”指**合并时段**（跨 7 个区间）；**单一 10 分钟区间上限恒为 833.333 kWh**（=5000 kW × 1/6 h）。

**附：保守裕度的量化价值（同口径对照，零额外求解）**

> **基准声明**：下表"相对**主口径**"列以 **13,252,341.09 元** 为分母；"裕度价值"另以**同对象**（均值点预报、裕度=0）为分母，二者不得混用。

| 方案（同：负荷当日已知；区别仅光伏预报与裕度） | 全年费用（元） | 相对**主口径** | 定位 |
| --- | ---: | ---: | --- |
| **★ 主口径：均值点预报 × 因果保守裕度** | **13,252,341.09** | — | **提交口径** |
| ① **均值点预报、裕度=0**（**同对象**对照） | 14,178,004.00 | +925,662.91（+6.98%） | 裕度价值 = **−925,662.91 元（−6.53%，分母=①）** |
| ② 4 情景 SAA、裕度=0（**不同对象**：情景集规划） | 13,396,023.55 | +143,682.46（+1.08%） | 仅作方法对照，**不得**用于算"裕度价值" |
| ③ 完全信息下界（计划直接用当天实际光伏，**不可执行**） | 12,254,696.55 | −997,644.54（**−7.53%**） | 即"**约 100 万元**"的预报误差不可消除代价 |
| ④ 旧灵敏度（同周4周情景集 + E1） | 15,238,244.28 | +1,985,903.19（+14.99%） | 信息受限口径 |
| ⑤ 附件1 典型日曲线 | 19,043,406.75 | +5,791,066.66（+43.70%） | 题面最直接给出的预报口径 |

> **口径澄清（重要）**：`13,396,023.55` 是"**4 情景 SAA**（用 4 天光伏逐日情景作规划、裕度=0）"，与主口径的"**均值点预报 + 裕度**"**不是同一类对象**；若要衡量保守裕度的价值，必须与 ①（同对象、裕度=0）比较，即 **−925,662.91 元（−6.53%）**。
> 备查：若以 4 情景 SAA 为分母，下界差为 `12,254,696.55 − 13,396,023.55 = −1,141,327.00（−8.52%）`；本节不采用该分母。


## 5. 必须披露的限度

1. **预报是自建的**：题面未给 Q2 的负荷/光伏预报，本文取"过去 4 天均值 + 因果保守裕度"。这是**预先定义、可复现**的规则（所有参数仅用 d 之前信息）；若改用它法（如附件1 典型日曲线），全年费用会升至 1,904 万元（附录对照）。
2. **完全信息下界**：若计划直接使用当天实际光伏（不具可执行性），全年 **12,254,696.55 元、紧急 0** —— 该口径下 5 倍机制与表3 双双退化，**故不作主结果**，仅作理想下界。
3. **剩余 100 万元 = 预报误差的不可消除代价**：非预见可达最低约 1,320.7 万元（同年度调参，仅好 0.34%，不可辩护），完美信息 1,225.6 万元。再向下压只能靠"使用未来信息"（即不可执行口径）或样本内调参，本文**主动放弃**。
4. **裕度的样本内对照**：固定 6%/7% 裕度（样本内最优）= 13,207,201 / 13,209,473 元；本文采用因果版 13,252,341 元，差 0.3%，**以可辩护性优先**。
5. **紧急购电的形态**：242/334 天发生（其余 92 天为 0），集中出现在 20:30 前后的晚高峰时段——与"预报偏低时由储能/紧急电补足"的机理一致；**不是每个时段都 >0，也不是全 0**。
