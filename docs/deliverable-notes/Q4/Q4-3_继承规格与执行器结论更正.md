# Q4-3 继承规格（依据 Q3 冻结清单 v0）+ 执行器结论更正

> 依据（唯一）：`D:\CMUCU\7.5对话\Q3冻结清单_v0_20260912.md`（**已获队长允许冻结**，Q3→Q4-3 的唯一继承依据）
> 参考（**不全信**）：`D:\CMUCU\7.5对话\外部方案\Q1-Q3终极建模方案_FINAL.md`（只借思路）
> 本文用途：**不依赖 `result3.xlsx` 的铺垫**——把 Q4-3 的接口与口径先钉死，`result3` 一到即可开工。

---

## 一、Q4-3 必须**直接继承**的项（逐条照抄冻结清单）

| 类 | 项 | 主值 |
| --- | --- | --- |
| 计费 | 读法 | **C**：`p·min(G⁰,A)+0.5p(G⁰−A)⁺+1.5p(A−G⁰)⁺+5p·H`；调整基准 = **日前计划 `G⁰`** |
| 时间 | 口径 | R 右端点，144 段/日，`dt=1/6` |
| 信息 | 负荷 | **视为已知**（论文显式声明为假设） |
| 信息 | 光伏 | **附件3 整点 = 该小时末端采样**；重建 = `slot_end`（末段=整点值 + 小时内线性插值 + **首小时用当天 τ−6 左锚** + 非负截断） |
| 终端 | 价值 | `λ_T = 0.4720`（终止时刻边际量） |
| 物理 | 储能 | SOC∈[1200,10800] **全时段**；≤833.333 kWh/段；**单向 η=0.9**；`S₀=6000`；**禁售电**；执行层 **χ=0** |
| 范围 | 窗口 | 2025-02-01~12-31（`d=31..364`，334 天） |
| 交付 | 列映射 | **位置映射 `col = 2 + t`**；表3 = 只列真实发生段、**日期列只在当日首段写一次**；纯文件夹 |
| **算法** | **计划层** | **v3 段级**：`δ_τ(d) = Q_0.8(过去 28 天同 τ 的"实现−预报"残差)`，逐日重估；`N̂ = (L − PV̂)·dt + δ` |
| 算法 | 额外裕度 | **m = 0**（附说明 `m*≈0.01`，区间 `[0.00,0.02]`） |
| 算法 | 识别性 | **只报区间 `α∈[0.10,0.25]`**，不报单点最优 |
| 算法 | 执行器 | **E1 主口径；v2b 平衡版作灵敏度（−2.57%(A) / −2.25%(C)）** |
| 算法 | 滚动 | **0/6/12/18 四时刻**；本块落定、未落定段改用本次解 |
| 算法 | 弃光上界 | `R ≤ PV̂·dt`（用**真实重建** PV̂，仪器不得改写） |

## 二、Q4-3 必须**替换**的项

| 项 | 从 | 到 |
| --- | --- | --- |
| 价格 | 附件1（逐日重复的固定分时曲线） | **附件4（逐日逐时段真实波动价）**，三处同步：计划费 `p·min(G⁰,A)`、调整费 `0.5p/1.5p`、紧急费 `5p·H` |
| 终端（**待定，需写明关系**） | `λ_T = 0.4720` | 若重标定为 `λ⁴`，**必须写明是"替换"还是"并存"**；默认按冻结清单**沿用 0.4720**，并把 `λ⁴` 作敏感性（我方已测：附件4 下扫描最优恰在 0.4720） |

## 三、Q4-3 **不得**做的事（冻结清单明令）

1. **不得把 Q2 的自建光伏预报带回 Q3/Q4-3** ⇒ Q4-3 的光伏输入**只能**用附件3 四发布时刻 + `slot_end` 重建；
2. **不得混用附件1/附件4 价格**（计划层与结算层必须同一套附件4）；
3. 不得改冻结的读法/映射/表3 写法/执行器主口径。

---

## 四、【重要更正】我此前的"执行器上界 1.0%"结论

**我此前说过**：用"固定计划 G、完美前视最小化紧急费"的 oracle 得到 **140,636 元 = 1.015%**，据此判断"执行器这条线基本到顶"。

**Q3 冻结清单的 O1 实测（v2b：−2.57%(A) / −2.25%(C)）推翻了这个判断**。原因很清楚，我当时也标注了限定词但没纠正结论：

- 我的 oracle **只允许执行器改 `C/D/H`，而计划量 `G` 被固定**（这正是 Q2/Q4-2 的结构：take-or-pay，计划量不变）；
- 而 **Q3/Q4-3 的结构里，执行器还能通过 `A`（实际取电量）影响读法 C 的三段结算**（`p·min(G⁰,A)`、下调 0.5p、上调 1.5p），杠杆明显更大。

⇒ **更正后的正确表述**：

| 结构 | 执行器杠杆 | 依据 |
| --- | --- | --- |
| **Q2 / Q4-2**（计划量 take-or-pay、G 固定） | 上界 **1.015%**（仅紧急费可动） | 我方 oracle 实验（`q4_probe_frontier.json`） |
| **Q3 / Q4-3**（承诺—调整结构，A 可变） | 实测 **−2.25%（读法 C）** | Q3 冻结清单 O1（v2b） |

**行动**：Q4-2 的"执行器上界 1.015%"**继续有效、写进论文**（限定在 Q2 结构）；
**Q4-3 一律继承 Q3 的执行器结论**（E1 主 + v2b 灵敏度），**不得**再用我那个 1% 上界去否掉 v2b。

---

## 五、现在就能做的铺垫（**不依赖 `result3.xlsx`**）

| # | 铺垫 | 产出 | 状态 |
| ---: | --- | --- | --- |
| A1 | 继承规格落盘（本文 §一~§三） | 本文件 | ✅ |
| A2 | 更正执行器结论并同步进方案与论文素材（§四） | 方案 §15 + `Q4_论文素材` | 待做 |
| A3 | **Q4-3 适配器接口规格**（不跑）：声明要 import 的模块与要替换的价格入口 | 见 §六 | ✅（规格） |
| A4 | Q4-3 价口径自检脚本（读附件4、断言 365×144、无负价；与 Q4-2 同源） | 复用 `q4_data_io.py` | ✅ 已有 |
| A5 | Q4-3 校验器骨架（从成品反算：读法 C 三段式恒等式 + 位置映射 + 表3 写法） | `code\q4_validator_q3struct.py` | 待写 |
| A6 | 交付说明模板（含"Q4-3 继承 Q3 冻结清单哈希"占位） | `交付说明模板_Q4-3.md` | 待写 |

---

## 六、Q4-3 适配器接口规格（**写明即可，`result3` 到位后再落地**）

```
【只读 import（不复制、不改写他人目录）】
  D:\CMUCU\7.5对话\code\q3_data_io.py        —— 数据入口/常量（S0、DAY0/DAY1、EPOCHS、LAM_T）
  D:\CMUCU\7.5对话\code\q3_pv_interp.py      —— slot_end 光伏重建（Q4-3 必须用同一重建）
  D:\CMUCU\7.5对话\code\q3_solver_billingC.py—— 读法 C 的求解器
  D:\CMUCU\7.5对话\code\q3_main_v2.py        —— 0/6/12/18 滚动主管线（v3 段级需求仪器）
  D:\CMUCU\7.5对话\code\q3_materialize.py    —— 物化（位置映射、表3 写法）
  D:\CMUCU\7.5对话\code\q3_validator.py      —— 校验器（对照用）

【Q4-3 的唯一改动点】
  价格张量：附件1 的 144 段常数曲线  ⟶  附件4 的当日 144 段（P4[d]）
  出现位置（三处，必须同时换）：计划费 / 调整费楔形 / 紧急费

【Q4-3 的隔离要求】
  输出目录：8对话\output\（不写他人目录）
  提交件名：result4-3.xlsx（模板只读、另存副本）
  λ：默认沿用 0.4720；把 λ⁴ 作敏感性并**写明两者关系**

【开工前置（硬）】
  ① 本继承规格落盘 ✅；② `7.5对话` 交付 `result3.xlsx` 与口径卡哈希；
  ③ Q4-3 的校验器骨架就绪；④ 队长"开跑 Q4-3"指令
```

---

## 七、与 Q4-2 的关系（**保持区别，不要混**）

---

## 八、【2026-09-13 追加】依据 7.6《Q3口径冻结 v1》+ `D:\CMUCU\Q3交付` 实体的三处修订与"留余地"要求

### 8.1 三处修订（相对本文 §一~§三）

| # | v0（7.5） | v1（7.6，队长 23:0x 已批） | 对 Q4-3 的影响 |
| ---: | --- | --- | --- |
| R1 | 读法 C | 读法 C **不变**，并新增代数恒等式 **`Cash_A − Cash_C = Σ p·(G⁰−A)⁺`** | Q4-3 校验器可加一条"读法敏感性恒等式"自检 |
| R2 | 计划层 = v3 段级（q=0.8）+ 额外裕度 m=0 | **(q,m) 二维网格标定**：**只报区间 + 标定点**；**禁止**写"q=0.8 最优/ q=0.5 更优/ 两者等价" | Q4-3 的计划层**继承 (q,m) 标定与区间表述**，主口径仍取标定点 (q=0.8, m=0) |
| R3 | Q3 头条数字（7.5 值） | **13,369,682.34 元**（C、q=0.8、m=0、334 天；`J_plan` 12,477,070.27 + `J_adj` 514,350.18 + `J_emg` 378,261.88） | **Q4-3 的对照基线改用此值**（7.5 的 13,456,394 作废） |

### 8.2 【实测确认】`result3.xlsx` 的「全天购电费」列（**原为"当日总费用"；2026-09-13 已按方案 A 统一**）

> **状态更新（2026-09-13，方案 A）**：Q3/Q4-3 两表该列已统一为 **本表购电量 × 该时段电价**（4 位小数，
> 计划表 = `Σp·G⁰`、调整表 = `Σp·A`）。本节下述"当日总费用"结论属**变更前的实测记录**，保留作为历史；
> 现口径见 `交付说明_Q3_v2.md` §一.4 与 `交付说明_Q4-3.md` §5（全年总费用仍为 Q3 13,120,194.06 / Q4-3 13,727,033.66）。

（历史记录）用本文档所属的体检脚本（`q4_validator_q3struct.py --price att1`）对变更前的 `result3.xlsx` 实测：

- 该列**全年合计 = 13,369,682.34**，恰好等于 Q3 的**总现金费用**（= `J_plan(净退口径)` + `J_adj` + `J_emg`）；
- 与"计划费 `Σp·G⁰`"或"`Σp·min(G⁰,A)`"**都不吻合**（全年绝对差 486,387 / 892,612 元），而与"净退计划费 + 调整费 + 当日紧急费"**吻合**（残差 = 0）。

**而 `result2.xlsx`（Q2）的同名列 = 仅计划费。**

⇒ **同名列在两问含义不同**（这正是队长所说的"防 AI 的烦人设定"之一）。

**Q4 的处置（2026-09-13 方案 A 之后）**：

| 交付件 | 表1「全天购电费」口径 | 论文 |
| --- | --- | --- |
| `result4-2.xlsx`（继承 Q2） | **本表购电量 × 附件4 电价**（`Σ p4·G`，未变） | 与其余三问同口径，可同轴核对 |
| `result4-3.xlsx`（继承 Q3） | **本表购电量 × 附件4 电价**（计划表 13,674,605.2675 / 调整表 13,081,228.4295） | 同上；总额见交付说明 §5 |

### 8.3 **留余地**：Q3 正在评估 v2b（执行层可回退）口径，Q4-3 必须容忍其**换版**

> 队长 2026-09-13 更新：**跨日腾挪不再留余地**（收益很小）；改为对 **v2b 口径决策**留余地。
> v2b = "实测与计划冲突时，允许执行层回退计划动作"；主线 `v3` 计划层上实测 **−1.28%(A) / −1.42%(C)**（>0.5% 门槛）。
> **一旦采用 ⇒ `result3.xlsx` 的调整量、充放电量、费用三者全变 ⇒ 换版交付；已交给对话 8 的那份作废。**
> 我方参与意见见 `Q4视角_v2b口径决策_参与意见.md`。

**Q4-3 的版本容忍设计**：

1. **不硬编码 Q3 的任何数字**：Q4-3 的对照基线、结构、映射一律从 **Q3 交付的口径卡 + `manifest.sha256.json` + `result3.xlsx` 的实际哈希**读取；Q3 一变，Q4-3 **按新哈希整体重跑**，禁止混用两个哈希下的数字；
2. **结构参数化**：若 Q3 的跨日腾挪落地为"跨日联合/多日窗口"，Q4-3 的**窗口与终端处理必须随之改变**（不是简单换价格）——这属于**结构变更**，必须重新走一遍 G2 闸门（换回附件1 复现新的 Q3 基线）；
3. **与 Q4-2 的边界照旧**：Q4-2（继承 Q2）**不受** Q3 跨日腾挪影响；两问仍分栏陈述、数字不互套；
4. **我方已有可直接支援 Q3 的证据**：我们独立测过"逐日 → 跨日联合"的代价——附件1 价下 **0.198%**、附件4 价下 **约 0.45%**（见 `q4_probe_lambda_joint.json`、`Q4_稳健性与裕度结论.md` §五-7），可作为其跨日腾挪收益的**量级参照**（不是替代他们的测算）。

### 8.4 体检结论（顺带）

用 `q4_validator_q3struct.py` 对 **Q3 实体交付**做 7 项体检：**6 项 PASS**（结构/表名/主表形状/动态表展开/表3 写法/块内限值/SOC 界与日界链）；
唯一 FAIL 是 **S2b**，且已查明为**口径差异（§8.2）而非错误** ⇒ 我方校验器已按**价格区制**参数化（`--price att1|att4`）；
**待补**：按"费用列口径"参数化（`--regime planfee|dailycash`），以匹配 §8.2 的两种同名不同义列（Q2 侧=计划费、Q3 侧=当日总费用）。

| 项 | Q4-2（= 重算问题二） | Q4-3（= 重算问题三） |
| --- | --- | --- |
| 信息 | 负荷已知 + **自建光伏预报**（过去4天均值 × (1−因果裕度)） | 负荷已知 + **附件3 四时刻预报 + `slot_end` 重建** |
| 结构 | 逐日计划（take-or-pay），无调整 | 0:00 承诺 `G⁰` + 6/12/18 调整，读法 **C** |
| 执行器 | E1 | E1 主 + **v2b 灵敏度** |
| 执行器杠杆 | 上界 **1.015%**（G 固定） | 实测 **−2.25%（读法 C）** |
| 结果 | **13,850,454.64 元**（已交付、已校验） | 待 `result3` |

⇒ 两问的**信息结构与算法结构都不同**，论文必须分栏陈述，**不得相互套用数字**。
