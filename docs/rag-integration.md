# 可选 RAG 集成

RAG 与本案例项目保持独立：不复制 RAG 源码、语料、索引或缓存，不要求安装 RAG 才能阅读模型、运行测试或执行本地审计。若使用独立部署的 RAG，配置文件只应通过环境变量传递命令和入口；示例见 `.codex/config.example.toml`。

检索可用于查找通用优化、信息集、非预见性、求解器诊断和敏感性分析方法。它不能取代题面、附件、当前输入或本地计算结果；无本案例专属命中时必须如实说明边界。
