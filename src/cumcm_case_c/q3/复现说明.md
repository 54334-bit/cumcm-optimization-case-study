# Q3 复现说明（版本 v2-2026-09-13）

## 一、环境

```powershell
$env:PYTHONPATH='D:\CMUCU\rag\.deps'      # scipy / numpy 来源（只读，不安装/升级）
$env:PYTHONIOENCODING='utf-8'
# 解释器：Python 3.12.14（codex-primary-runtime）
```

## 二、脚本清单与用途

| 脚本 | 用途 |
| --- | --- |
| `q76_v2b_full.py` | **全年 334 天重跑 + 物化 + Y1–Y4 自检**（本版提交件的直接来源） |
| `q76_v2b_mini.py` | v2b vs E1 的 60 天 mini 对照（换版前置证据） |
| `q76_jsonl_to_days.py` | 逐段 JSONL → `{"days":[…]}` 转换（物化输入格式） |
| `q76_materialize.py` | 物化引擎驱动（**强制 `BLOCK=24` = 4 小时段**） |
| `q76_fill_four_dates.py` | 论文表 1/2/3 四日期直出（**表 3 需前向填充日期列**） |
| `q76_gate.py` / `q76_check_blockfix.py` | 放行闸门自查 / 时段块一致性检查 |
| `q76_scan.py` | 递归全量扫描 + 禁引清单 |
| `q76_e4_alignment.py` | 同保守度对齐与边际传导分析（论文用） |

## 三、复现命令

```powershell
python q76_v2b_full.py run          # ⚠️ 会覆盖提交件；复现请先把 out 指向副本路径
python q76_gate.py                  # 对提交件跑放行闸门
```

## 四、禁忌

1. **不要重跑 `q76_v2b_full.py`**（覆盖 `result3_v2.xlsx`）；
2. 不要用 xlsx 字节 sha 做版本判据（zip 时间戳导致不稳定），请按**单元格值/逐段真值**比对；
3. 不要修改 `blocK`（4 小时段）与 `col=2+t` 映射——这两处是本项目踩过的坑（见 `04_证据` 的相关勘误与组件哈希文件）。
