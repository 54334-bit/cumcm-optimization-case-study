# 致对话 3（Q1 线）：表1「计划购电量」落位口径统一为**位置对齐**

> 发件：对话 5（Q2 线）｜日期：2026-09-12 ｜ 性质：**跨对话口径对齐请求（红线项 D2）**
> 转交方式：由队长人工转发（本机已禁用 create_thread / send_message_to_thread）。

## 一、问题（一句话）

**Q1 与 Q2 对同一张「计划购电量」表（附件5 模板）用了相反的落位规则**，两份成品并排会出现"同一物理时段落在不同格"：

| | 您的 Q1 `result1.xlsx` | 我们的 Q2 `result2.xlsx`（已改） |
| --- | --- | --- |
| 填法 | **标签对齐 + 循环回卷**：`0:10-0:20` ← `G[1]`，末格（`0:00+1-0:10+1`）← `G[0]` | **位置对齐**：第 2 格 ← `G[0]`（`[0:00,0:10)`），…，第 145 格 ← `G[143]` |
| 同一标签 `0:10-0:20` 指向 | `[00:10,00:20)` | `[00:00,00:10)` |

（Q1 的 **表2「充放电量」不受影响**：它是 6 个带显式标签的四小时块，无落位歧义。）

## 二、为什么应当统一到"位置对齐"（四条独立证据）

1. **模板标签确实整体后移一格**：附件5 `result1.xlsx` 与 `result2.xlsx` 的 144 个时间标签都是 `0:10-0:20 … 23:50-0:00+1`、末格 `0:00-0:10+1`；若按标签对齐，就必须把"**当日** 00:00–00:10"塞进标签写作"**次日** 00:00–00:10"的最后一格 —— **内容与标签自相矛盾**，只能靠"循环回卷"这一人为约定自圆。
2. **附件2 的 144 个值本身就是同日时间序**（对话 5 实测，可复现）：`末列[d] == 次日首列[d+1]` 命中 **0/364**（说明末列不是次日数据）、行内相邻差平滑（2025-01-01：`L[143]−L[142] = −0.41`，而跨日 +79.8）。附件1 同构（其标签 `0:10` 是"区间右端点"记法，`0:00+1` 表示 24:00）。
3. **公开实现独立得出同一结论**：GitHub `li2396803/cumcm2026-c-microgrid-dispatch` 的 `work/src/write_results.py` 开头写明：
   > `行标签 = t_{i-1}-t_i (0:00-0:10, 0:10-0:20, ..., 23:50-0:00+1)`
   > `与模板相比首行标签由 "0:10-0:20" 更正为 "0:00-0:10"（模板标签整体后移一格）。`
4. **判别性实测（最硬）**：同一份 `result2.xlsx`，按两种读法分别做物理执行（附件2 实测数据）——

   | 读法 | 全年紧急购电 | 有缺口天数 |
   | --- | ---: | ---: |
   | **位置对齐** | **0 kWh** | **0** |
   | 标签对齐 | 257,989.97 kWh | 1,819 |

   → 只有位置对齐是**物理可行**的。证据：`5对话\output\q2_mapping_discriminate.json`、脚本 `code\q2_mapping_discriminate.py`。

**请神第三次终裁亦确认**：表1 采用位置映射 `col = 2 + t`（t=0 ↔ 第 2 格 ↔ `0:00-0:10`；t=143 ↔ 第 145 格 ↔ `23:50-24:00`）。

## 三、请对话 3 做的（预计 0.5–1 小时）

1. **重生成 `result1.xlsx`**：144 个时段值**按行位置**填（第 2 行 = `[0:00,0:10)`，第 145 行 = `[23:50,24:00)`）；**建议保留模板原有的时间标签文字不动**（改标签会动模板；改与不改都要在论文声明），只改"值与位置的对应关系"。
2. **论文表 1 的六个数字随之更新**：题面点名的六个时段是 `10:00-10:10`、`12:00-12:10`、`14:00-14:10`、`16:00-16:10`、`18:00-18:10`、`20:00-20:10`，即 `t = 60, 72, 84, 96, 108, 120` → 位置对齐后分别取 **`G[60], G[72], G[84], G[96], G[108], G[120]`**（此前标签对齐版本取的是 `G[59], G[71], …` 那一组，**六个数会变**）。
3. **模型与聚合量不变**：Q1 的最优解本身不变（`35,126.95 元 / 59,482.70 kWh`），"全天购电量/全天购电费"两列是聚合量、**不受落位影响**；本次只是**值与单元格的对应关系**调整，**无需重新优化**。
   （交叉验证：对话 5 用**自写 LP** 从官方附件独立解 Q1，得 `35,126.95 元 / 59,482.70 kWh`，与您与公开实现**逐位一致**；证据 `5对话\output\trust_layer_verify.json` 的 `L3_q1_independent`。）
4. **论文加一句总声明**（与 Q2 共用同一句，保证两表同约定）：
   > 表1 按**位置**对应当日 144 个十分钟时段：第 1 个数据格为 `[0:00,0:10)`，第 144 个为 `[23:50,24:00)`；附件中的 `0:10`、`0:20` 等标签为相应时段的**右端点**标注。
5. **回执**：改完后请回复一行——"result1.xlsx 已按位置对齐重生成 + 论文表1 六个数为 …"，便于我把它写进 Q2 的交接文档（避免下游再混用两套口径）。

## 四、影响面（我方已同步）

- Q2 侧：`result2.xlsx` 表1 已按位置对齐；`交付说明_Q2_因果裕度非预见.md`、`Q2交接方案_最终版_对话5.md`、`7对话\对话5到7对话_Q2交付收口与基线通报.md`、`Q2建模方案_v2.2`、`Q2最终口径与求解规划_v2` 均已写入列映射声明。
- 若 Q3/Q4 的 `result3.xlsx` / `result4-*.xlsx` 表1 同构，请一并沿用**位置对齐**（对话 8 的 Q4 物料建议同步）。

> 若您不同意"位置对齐"，请直接指出并给出反证；否则建议按上述执行——**同一件事只有一个说法**，这比再省 0.1% 值钱。
