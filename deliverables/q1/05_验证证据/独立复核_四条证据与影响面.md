# Q1 表1落位口径「四条证据」独立复核 + 下游影响面扫描（reviewer R3）

复核人：reviewer R3（只读、只复算，未修改任何被审文件）
复核日期：2026-09-12 ｜ 被复核主张：表1 落位由「标签对齐」改为「位置对齐」（R-20260912-1）
复核对象：`D:\CMUCU\3对话\result1落位口径修订记录_位置对齐.md` 的四条证据 + 下游影响面

---

【角色】reviewer R3（证据与影响面）

【1】模板标签后移一格：**成立**

实读 `D:\CMUCU\赛题\C题\附件\附件5\result1.xlsx`「计划购电量」sheet（A1:B145）：

| 单元格 | 内容 |
| --- | --- |
| A1 | `时间段`（表头） |
| A2 | `0:10-0:20` |
| A3 | `0:20-0:30` |
| A144 | `23:50-0:00+1` |
| A145 | `0:00+1-0:10+1` |

* 数据行 **144 行（A2:A145）**，逐格打印全列，**无空值、无异常单元格**，全部是连续 10 分钟区间标签。
* 与自然日序列 `0:00-0:10 … 23:50-0:00+1`（144 项）逐项比对：模板第 k 行标签 = 自然序列第 k+1 项，
  首格由 `0:00-0:10` 变成 `0:10-0:20`，末尾多出的 `0:00+1-0:10+1` 正是次日第一格 → **整体后移一格**。
* `附件5\result2.xlsx`「计划购电量」同构：147 列 = 1 列日期 + 144 个时段列 + `全天购电量`/`全天购电费`；
  时段列首格 `0:10-0:20`、第 144 格 `0:00-0:10+1`。335 行 = 1 表头 + 334 个日期（2025-02-01…12-31）。
* 结论：**成立**。附注（低）：result2 末格文字为 `0:00-0:10+1`，与 result1 的 `0:00+1-0:10+1` 不同，
  两模板该格文字本身不一致；不影响结论，但修订记录把两者写成同一文字属转述不精确（见【6】出入 2）。

【2】附件同为当日序：**成立**（且这是「位置对齐」成立的直接前提）

* `附件1.xlsx` Sheet1（A1:D145）：表头 `时间/电价/小区负载/光伏发电预测功率`，数据 144 行；
  时间列首值 `0:10`（time 00:10）、末值 `0:00+1`，10 分钟递增，**无 0:00 行**。
* `附件2.xlsx` 两个 sheet（`小区负载`、`光伏发电实际功率`）均 366 行 × 145 列：
  行 = 日期 2025-01-01…2025-12-31（365 天），列 = 1 列日期 + 144 个时段列，首列 time(0,10)、**末列 `0:00+1`**。
* 判断：144 个值确为**同日时间序**。唯一自洽读法是「标签 = 区间右端点」：
  `0:10`↔[00:00,00:10)、…、`0:00+1`(=24:00)↔[23:50,24:00)，144 个标签**恰好覆盖自然日一次**；
  若按左端点读，则 [00:00,00:10) 无对应数据、且凭空多出次日首格，与 365×144 的结构不符。
* 结论：**成立**（`0:00+1` 即 24:00，非次日首列）。

【3】公开实现原文：**成立**（联网成功，非猜测）

`https://raw.githubusercontent.com/li2396803/cumcm2026-c-microgrid-dispatch/main/work/src/write_results.py`
（HTTP 200，正文 7010 字节，正文 SHA256 `3B9398E5EA6F6749E32AC4D034DC698995E9D4979B8E3CA4E8F6F1A2B17D3186`）顶部 docstring 原文：

```
"""按附件 5 模板写出 result1/2/3/4-2/4-3.xlsx 结果文件.

行标签约定: 第 i 行(时间段) 使用附件中标签为 t_i 的记录, 即
    行标签 = t_{i-1}-t_i (0:00-0:10, 0:10-0:20, ..., 23:50-0:00+1)
与模板相比首行标签由 "0:10-0:20" 更正为 "0:00-0:10"(模板标签整体后移一格)。
"""
```

* 该 docstring 与第【1】条实读完全互证：模板首行标签确为 `0:10-0:20`，作者按 `0:00-0:10…` 的行序写出（即按位置），
  并自行把差异定性为「模板标签整体后移一格」；同文件 `slot_labels()` 生成 `0:00-0:10 … 23:50-24:00`。
* 修订记录 §二 对该句的引用**逐字准确**（含全角括号）。
* 限定：这是第三方公开实现（MIT），属**旁证**，不是赛事权威；其价值在于与官方附件结构互证，不宜在论文中当作依据引用。
* 结论：**成立（支持位置对齐）**。

【4】判别性实测：数字一致、结论**成立**；但引用口径有两处不精确

`D:\CMUCU\5对话\output\q2_mapping_discriminate.json` 实读：

| 读法 | 紧急购电总量 | days_with_shortage | 单区间最大缺口 | 期末储电量 |
| --- | ---: | ---: | ---: | ---: |
| positional（位置对齐） | 0.0 kWh | 0 | 0.0 kWh | 6000.0 |
| label_aligned（标签对齐） | 257989.974 kWh | 1819 | 880.08 kWh | 6069.61 |

* 与修订记录/最终方案引用的 `0 / 257,989.97 / 1,819` **一致**。
* **单位问题（须改）**：源脚本 `D:\CMUCU\5对话\code\q2_mapping_discriminate.py` 中
  `DAYS = list(range(31,365))` 即 **334 天**（附件2 行 33…366 = 2025-02-01…12-31，**不含 1 月**），
  `short_days` 在每个（天, 时段）出现 >1e-3 kWh 缺口时 +1 → `1819` 是「天×时段」计数（上限 334×144=48096），
  **不是 1,819 天**；且窗口不含 1 月，称「全年」亦不准确。
* 建议表述：「在 2025-02-01…12-31 共 334 天 × 144 时段中，位置读法 0 个缺口时段、标签读法 1,819 个缺口时段、合计紧急购电 257,989.97 kWh」。
* 结论：判别结论（位置读法物理可行、标签读法不可行）**成立**；数字引用正确，**单位/窗口表述不准确**。

【5】下游影响面

**A. 须改清单（现行文档/交付件仍把旧口径或旧 SHA 当现行）**

| # | 位置 | 事实 | 等级 |
| --- | --- | --- | --- |
| 1 | `3对话\Q1价值层_交付包_20260912\02_结果JSON\q1_checkpoint.json` | SHA `8E43ECD3…E9FF24`（17465 B），`table1.rows` 仍为 **`row_60…row_120`（旧口径键名）**；而同包 `06_...\result1.xlsx(现行-位置对齐)` 为 `D7F90410…C6CFE2`。包内自相矛盾 | **严重** |
| 2 | `3对话\Q1价值层_交付包_20260912\manifest.sha256.json` | 把 `02_结果JSON\q1_checkpoint.json` 的校验值**固化为旧 SHA `8e43ecd3…`**；`hash(现行 checkpoint)=CDC1BEEB…` 无法通过该 manifest | **严重** |
| 3 | `3对话\现有方案_Q1求解与价值层.md` §3.2（L50 起） | 标题仍为「result1 物化映射：**方案 A（按标签对齐 + 循环回卷）**」，正文给 `B(2+i) = G[(i+1) mod 144]`，**节内无作废标记**（修订说明远在 L213-228 §八） | 中 |
| 4 | `3对话\交接清单_对话4.md` L52 第 6 条 | 仍写「按 **方案 A（按官方模板标签对齐）** + 周期边界…末行 `0:00+1-0:10+1`←G[0]」；顶部 L5 有修订横幅，但**条目本身未标注作废** | 中低 |
| 5 | `3对话\Q1最终方案.md`（横幅 + §2 证据 4）、`result1落位口径修订记录_位置对齐.md` §二证据4、`3对话\回复对话5_Q1表1已改位置对齐_呈队长转交.md` | 「缺口 **1,819 天**」单位错误、「全年」实为 334 天 | 中低 |

注：`现有方案…md`、`交接清单…md` 在交付包 `03_文档\` 下各有同名副本，经 SHA256 比对与根目录**同一份**（同哈希），
故修一处须两处同改（或重生成交付包）。

**B. 历史留痕清单（保留合理，不需改）**

* `3对话\README.md`、`3对话\Q1价值层_交付包_20260912\03_文档\3对话_README_执行计划.md`：
  顶部已加显式横幅「§六.1 与 §七.1 关于方案 A 的裁定**已作废**…下文保留原始记录（审计留痕），不得再据此填表」，
  正文 L57/L65/L76 保留原裁定 → 符合「留痕 + 显式作废」，**合理**。
* `Q1最终方案.md` §2.1「原『方案 A』记录（**已作废，仅留痕**）」、L168 删除线行。
* `3对话\code\materialize_result1.py`：文件头已加「⚠️ 已作废…请改用 `materialize_result1_pos.py`…勿再运行或引用」→ 合理。
* `3对话\output\_SUPERSEDED_q1_label_readback.md`、`q1_label_readback.json`、`q1_materialize_report.json`（旧读数）、
  `_result1_labelaligned_backup.xlsx`（修订前副本）→ 留档，合理。
* `15DE5D10…EECB71` 的**其余**出现处均为「修订前留档」语义：`manifest.sha256.json`（键名已标 `(修订前-标签对齐)`）、
  交付包 `README.md` L120、`交接清单_对话4.md` L20、`现有方案…md` L220、修订记录 §五、回复回执 L69 → 合理。
* 仲裁/外部意见留痕：`_请神_*.txt`、`请神_*.txt`、`豆包_*.txt`、`复审整改清单_助手.md`、`收口补齐清单_助手.md`、
  `A对话到3对话_Q1修正交接.md` → 历史过程记录，合理保留。
* `Q1价值层_交付包_20260912\04_独立复现_reviewer\任务书_q1_vl_supervisor_final{,2}.md`：
  其中「红线 SHA 仍为 `15DE5D10…`/`8E43ECD3…`」是**当时**下发给复现者的指令，属历史留痕；
  但**建议加一行注记**「该红线已于 R-20260912-1 修订（result1→D7F90410、checkpoint→CDC1BEEB）」，
  否则后续复现者按旧 SHA 会误判 FAIL（见优化建议 O4）。
* `4对话\`：全目录扫描仅 1 处命中「方案 A」，为 `output\FIGURES_VERIFY.md` L208 的**论文图件字体方案**，与 result1 落位无关；
  **4对话无旧 SHA、无「标签对齐」引用** → 干净。

扫描口径：`3对话\`、`4对话\` 递归 1376 个 `.md/.txt/.json/.py/.csv`，模式 `15DE5D10`、`8E43ECD3`、`标签对齐`、`方案A`、`方案 A`、`循环回卷`。

【6】修订记录与磁盘是否相符：**有 3 处出入**（核心断言全部相符）

相符项（逐条复算）：

* §2.1 错位自查：`G[0]` 旧文件在第 145 行（1406.6411）= 现行第 2 行；`G[1]` 旧在第 2 行（1406.32986667）；
  `G[143]` 旧在第 144 行（574.12098333）= 现行第 145 行。且逐行验证旧文件满足 `old[r] = G[r-1 mod 144]`（r=2..145 全通过）→ **一致**。
* §2.2「六个数不变」：按**正确读法**（新文件取第 `t+2` 行、旧文件取标签所在第 `t+1` 行）逐位比对
  `t=60/72/84/96/108/120` → `0 / 480.412450 / 0 / 445.431650 / 531.894050 / 0`，**六项全同** →
  该断言**成立**（若错误地按同一行号比对，会得到 3 处不同，属读法误用）。
* §五 SHA：现行 `result1.xlsx` = `D7F90410776BFE31…C6CFE2` ✓；留档 `_result1_labelaligned_backup.xlsx` = `15DE5D10EC5DD234…EECB71` ✓；
  现行 `q1_checkpoint.json` = `CDC1BEEB6B07DAAF…C9F5F3` ✓ → **三项全部与磁盘一致**。
* 「已在 `Q1最终方案.md` §2.0 收录论文口径声明」✓（§2.0 确有位置对应 + 「附件标签为右端点」声明；
  且交付包 `03_文档\Q1最终方案.md` 与根目录同名文件 SHA256 同为 `9300D07E…DA0A`，确为同一份）。
* `code\materialize_result1_pos.py`、`code\q1_readback_pos.py`、`output\q1_label_readback_pos.json`（六时段 `ok=true`、`errors=[]`）、
  `output\q1_materialize_report_pos.json` 均存在；`q1_readback_pos.json` 记录 `table1_check` 六行与 checkpoint 逐位一致，`sumG_read=59482.6989153` ✓。

出入项：

1. **（须改，与记录直接冲突）** 记录 §三 与回复回执称「`Q1价值层_交付包_20260912/` 已同步」「checkpoint SHA 已由 `8E43ECD3…` 变为 `CDC1BEEB…`」，
   但**包内 `02_结果JSON\q1_checkpoint.json` 实为 `8E43ECD3…`（17465 B，`table1` 键名 `row_60…row_120`）**，即修订前版本；
   `manifest.sha256.json` 同之。→ 包内文件、manifest 与回执三者互相矛盾。
2. **（低）** §二证据 1 把 result1/result2 末格文字统一写成 `0:00+1-0:10+1`，实读 **result2 为 `0:00-0:10+1`**。
3. **（低）** §二证据 4 写「缺口 **1,819 天**」「全年」，实为 334 天内 1,819 个**时段**缺口（见【4】）。

【结论】证据侧 **PASS**；交付同步项 **FAIL**

* 四条证据（1 模板后移一格、2 附件同为当日序、3 公开实现原文、4 判别性实测）**经独立复核全部成立**，
  其中第 2 条我给出「右端点读法是唯一自洽解」的独立论证，第 3 条已联网取到原文并核验引用逐字准确。
* `result1.xlsx` 的位置对齐修订**与磁盘逐位相符**，最优解数值未变（`J_actual=35126.948624416575`、`Q_G=59482.698915258756`），
  修订记录的核心断言成立，**该修订可以执行**。
* 但「已同步交付包」这一断言**与磁盘不符**（包内 checkpoint 旧键名 + manifest 固化旧 SHA），
  属交付件内部自相矛盾，**须在交付前修掉**；同时把「1,819 天」改为「1,819 个缺口时段」。

---

## 严重问题

**S1（交付一致性）** `3对话\Q1价值层_交付包_20260912\02_结果JSON\q1_checkpoint.json` 仍是修订前版本
（`8E43ECD3…`，`table1.rows = row_60…row_120`），同一交付包内的 `06_...\result1.xlsx(现行-位置对齐)`
却是修订后版本（`D7F90410…`），且 `manifest.sha256.json` 把旧 SHA 固化为校验值。
后果：评阅/复现者按包内 manifest 校验或按包内 checkpoint 读 `table1` 键名，会与包内 result1 的现行口径直接冲突，
并推翻修订记录 §三「已同步交付包」的陈述。
建议：① 用现行 `3对话\output\q1_checkpoint.json`（`CDC1BEEB…`）覆盖包内 `02_结果JSON\` 同名文件；
② 重算并重写 `manifest.sha256.json`；③ 在 manifest 增 `revision: R-20260912-1` 字段；
④ 交付包 `README.md` 的红线 SHA 表同步为 `D7F90410…`/`CDC1BEEB…`。

## 需要改进

**I1（单位/窗口口径错误，已扩散到 3 份现行文档）**「缺口 1,819 **天**」「**全年**」出现在
`Q1最终方案.md`（顶部横幅 + §2 证据 4）、`result1落位口径修订记录_位置对齐.md` §二证据4、
`回复对话5_Q1表1已改位置对齐_呈队长转交.md`。
建议统一改为「2025-02-01…12-31 共 334 天 × 144 时段中 1,819 个缺口时段；合计紧急购电 257,989.97 kWh」。

**I2** `现有方案_Q1求解与价值层.md` §3.2（及其交付包副本）标题与正文仍把「方案 A（标签对齐 + 循环回卷）」作为映射叙述，
节内无作废标记。建议在该小节首行插入一行：
`> ⚠️ 本节口径已于 2026-09-12 作废（R-20260912-1），现行见 Q1最终方案.md §2.0。`

**I3** `交接清单_对话4.md` 第 6 条（及其交付包副本）仍以现行语气写方案 A。建议条目内就地加「（已作废，见修订记录）」，
避免下游只读到条目、漏读顶部横幅。

**I4** 证据 3 引自第三方公开实现。修订记录 §六 用「更硬」形容该证据，措辞容易被读成权威依据。
建议改为「与官方附件结构互证（旁证）」，并在论文口径说明中只引用附件事实与位置对应关系。

## 优化建议

**O1** 两份模板末格文字不一致（result1 `0:00+1-0:10+1` vs result2 `0:00-0:10+1`）。建议在交付 README 明确一句
「模板 A 列/首行标签一律保持原样，任何人不得『顺手改正』」，防止后续再次出现落位漂移。

**O2** 建议随 result1 落盘一个机器可校验的映射声明（如 `mapping_pos.json`：`row → t → [start,end)`），
并让 `q1_readback_pos.py` 以该声明而非硬编码行号做校验，降低再次错位风险。

**O3** 建议在 `5对话\code\q2_mapping_discriminate.py` 中把字段 `days_with_shortage` 更名为 `slot_shortfall_count`，
并新增 `horizon`（起止日期/天数）字段，从源头消除下游误引单位。

**O4** 建议在 `04_独立复现_reviewer\任务书_q1_vl_supervisor_final.md` / `final2.md` 的红线行补注：
「该红线已于 R-20260912-1 修订（result1 → `D7F90410…C6CFE2`、checkpoint → `CDC1BEEB…C9F5F3`）」，
避免后续复现者按旧 SHA 判 FAIL。

---

复核方式与可复现命令要点：
* 模板/附件实读：`openpyxl` 逐格读取（`附件5\result1.xlsx` A 列全列、`附件2.xlsx` 表头首末列、`附件1.xlsx` 首末行）。
* 错位与六时段：`result1.xlsx` vs `_result1_labelaligned_backup.xlsx` 逐行比对 + `q1_checkpoint.json` / `q1_label_readback_pos.json` 交叉。
* 公开实现：`Invoke-WebRequest` 取 raw 正文（HTTP 200，正文 7010 B，SHA256 `3B9398E5…3186`）。
* 影响面：`3对话\`、`4对话\` 递归 1376 文件 × 6 关键词 + 旧 SHA 精确匹配。
* 未使用 `view_image`；未修改任何被审文件（本报告为唯一新增文件）。
