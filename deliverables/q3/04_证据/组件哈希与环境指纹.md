# 组件哈希与环境指纹（Q3 v2 交付证据）

> 生成：2026-09-13　｜　用途：证明"跑的就是被验证过的那个执行器"，并让复现可指认

## 一、被 import 的主线模块（7 模块聚合哈希）

聚合哈希（`k:sha256` 拼接后取 sha256）：**`4ddf3879c220a617d2a97876517988a1f5248318792ede0359b4ce18c7e9379f`**（前 12 位 `4ddf3879c220`）
跑前 = 跑后 = 该值，`hash_changed_files: []`。

## 二、★ 组件哈希（终检 S1：此前缺失，现补齐）

| 组件 | 路径 | sha256 |
| --- | --- | --- |
| **执行器（本次升级对象）** | `7.5对话\code\q3_exec_v2b.py` | `fa1d86f01f6616aa517a87cd9d11a09cc5b000abdf224687739fc0e659d52a00` |
| 物化引擎 | `7.6对话\code\q76_materialize.py` | 见 `manifest.sha256.json` |
| JSONL→`{"days":[…]}` 转换器 | `7.6对话\code\q76_jsonl_to_days.py` | 见 `manifest.sha256.json` |
| 全年驱动脚本 | `7.6对话\code\q76_v2b_full.py` | 见 `manifest.sha256.json` |

> 执行器 mtime = 2026-09-12 18:40，**早于** mini 实验与全年重跑 ⇒ 两次运行使用的是同一个执行器文件（自洽）。

## 三、环境指纹

| 项 | 值 |
| --- | --- |
| 解释器 | 运行时 Python **3.12.14**（`codex-primary-runtime`） |
| 科学计算依赖来源 | `D:\CMUCU\rag\.deps`（scipy / numpy，**只读使用，未安装/升级**） |
| 电价源 | 附件1（典型日，144 段）；与 `B对话\clean\q1_clean.csv` 交叉核对一致 |
| 窗口 | `d=31..364`（2025-02-01~12-31，334 天） |

## 四、复现与禁忌

```powershell
$env:PYTHONPATH='D:\CMUCU\rag\.deps'   # scipy/numpy 来源
$env:PYTHONIOENCODING='utf-8'
python D:\CMUCU\7.6对话\code\q76_v2b_full.py run
```

⚠️ **该命令会覆盖 `result3_v2.xlsx` 并改变其字节 sha** ⇒ 端到端复现请**先把输出改到副本路径**。

## 五、附加论据（终检 M5）

旧版 **E1** 全年有 **506 段** 出现 `R_G > A`（最大 66.46 kWh，例 2025-11-07 第 38 段 `A=0、R_G=66.46`），根因是 E1 在实测富余段仍执行计划放电；**v2b 为 0 段**。
说明：`R_G` **不是交付件任何一列**，只影响内部账与论文叙述自洽——**不改变已提交表的数值**，但支持"v2b 在物理一致性与成本上同时优于 E1"。
