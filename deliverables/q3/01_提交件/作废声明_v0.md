# 作废声明（Q3 结果文件 · v1 → v2）

> 生成：2026-09-13　｜　发布：7.6 对话（C 题 Q3 线）　｜　状态：**生效**

## 一、作废对象

| 文件 | sha256 | 处置 |
| --- | --- | --- |
| `01_提交件\result3.xlsx`（v1，E1 执行器口径） | `9159846FC1D91E5EED23C1CF91C10DEDB9C21C1283A82D7B559FE213D54EB9A3` | **作废**，已移入 `04_证据\_void\result3_VOID_v1.xlsx` |
| `03_论文素材\论文表1表2表3_四个指定日期.md`（v1，由 v1 成品生成） | `387C6EE90B1A1B033B1869DFECB92A763AF41FF7F0BED496FC312B21BE61629C` | **作废**，已移入 `04_证据\_void\..._VOID_v1.md` |

## 二、作废原因

Q3 的执行器由 **E1**（纯前向规则）升为 **v2b**（实测与计划冲突时允许执行层回退计划动作），该变更经：
① 60 天 mini 实验（`J_cash` −1.8522%，四项判据全过）；
② 全年 334 天重跑 + 重物化（`J_cash` **−1.8661%**，Y1–Y4 全过）；
③ 独立终检复核（关键数字逐位复现；并确认同一次重跑的 **E1 臂逐位复现了 v1 交付件，差 0.0**，证明两版唯一差别是执行器）。
⇒ **v1 的全部数字不再有效**，任何文档、图表不得引用。

## 三、版本切换

| 项 | v1（作废） | **v2（有效）** |
| --- | ---: | ---: |
| 执行器 | E1 | **v2b** |
| 全年购电费用（元） | 13,369,682.34 | **13,120,194.06** |
| 紧急购电量 ΣH（kWh） | 95,806.02 | **42,672.2** |
| 提交件 | `result3.xlsx` | **`result3_v2.xlsx`**（sha256 `39F4C8DF…`） |

## 四、使用须知（**必须遵守**）

1. **禁止 v1/v2 混用**：论文、交付说明、图件、交接清单一律以 **v2 的 `13,120,194.06`** 为准；
2. **`xlsx` 的字节 sha256 不稳定**（同内容重跑时 zip 容器时间戳变化会导致 sha 变化）⇒ **版本标识与验收请按"单元格值 / 逐段真值"比对**，不要以 xlsx 字节 sha 作为唯一凭据；
3. **禁止重跑 `q76_v2b_full.py`**（会覆盖 `result3_v2.xlsx`）；端到端复现请在**副本路径**上运行；
4. 下游（Q4-3）必须按 **v2 哈希 `39F4C8DF…`** 整体重跑，不做"只换价格"的简化。
