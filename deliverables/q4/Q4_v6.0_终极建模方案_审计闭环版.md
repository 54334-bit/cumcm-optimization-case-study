# 2026 高教社杯 C 题——问题四终极建模方案 v6.0

## 波动电价下的因果采购、树结构联合场景与价格条件储能状态价值

> **文件性质**：最终实施型建模方案，不是论文正文。  
> **组织框架**：数据预处理 → 建模 → 求解与验证 → 灵敏度分析（简）。  
> **迭代模式**：问题检索 → 问题修复 → 可证伪验证；修复过程中产生的新问题重新入队。  
> **效力顺序**：题面 > `口径统一文件_Q1-Q3_v3.0_融合版.txt` > 本方案 > 历史方案/审计意见 > `一些易错点.txt` 中未与高优先级口径冲突的提醒。  
> **正式评价期**：2025-02-01—2025-12-31，共 334 天；2025 年 1 月仅用于 burn-in、shadow pilot、prequential 选择与参数冻结。  
> **版本**：v6.0；上一版 SHA-256：`ae11abf0c45b828f03d7068a414d54460c3e3ac3d34ae48acf0f3cc3d09c9b4e`。  
> **数值纪律**：Q4-2/Q4-3 的费用、紧急电量、PWL gap、VOI 等全部“待实算”；不得把旧草案粗略金额当正式结论。

---

# 0. 本轮终裁与问题修复总表

## 0.1 全局配置冻结

正式主配置必须显式写入 manifest：

```text
PRICE_VISIBILITY_MAIN      = CAUSAL_PRICE
PRICE_VISIBILITY_SENS      = KNOWN_DAY_PRICE

FORMAL_Q2                  = FROZEN_Q2_DAILY_LP
FORMAL_Q3                  = CONDITIONAL_SAA_LDR_MPC_PWL

FORMAL_Q3_EXECUTOR         = MPC_TWO_MODE
LEGACY_Q3_EXECUTOR         = E1_LEGACY_ONLY

QM_ROLE_Q42                = FROZEN_Q2_FORECAST_INTERFACE
QM_ROLE_Q43                = BASELINE_ONLY

Q3_SETTLEMENT_MAIN         = C
Q4_YEAR_END_HARD_6000      = FALSE
RESULT_MAPPING             = POSITION_B2_PLUS_T
```

其中：

- `QM_ROLE_Q43=BASELINE_ONLY`：冻结的 \((q,m)\) 只用于 Q3 历史标定点 replay、deterministic/point baseline 与敏感性；**不进入 Q4-3 正式 conditional SAA**，避免与场景分布风险重复保守。
- `E1` 只用于 legacy replay / sensitivity；正式 Q4-3 executor 继承上位融合口径的 MPC + two-mode recourse。
- 读法 C 为唯一主结算命名。

## 0.2 易错点的采纳边界

### 保留为硬红线

- kW → kWh/slot：
  \[
  E=P/6.
  \]
- 时间标签与槽索引错位为 fatal；
- SOC 始终：
  \[
  1200\le S_t\le10800;
  \]
- 单槽充/放电功率上限：
  \[
  5000/6=833.333333\text{ kWh};
  \]
- 实际执行：
  \[
  C_tD_t=0;
  \]
- 不默认售电；
- 计划购电、调整合同、紧急电分变量、分费用；
- 6/12/18 不修改已经执行时段；
- result4-2/result4-3 单位、映射、紧急区间必须双向回读。

### 明确否决的旧口径

1. “能量平衡必须写 ≥”不是本方案最终口径；加入弃光、未调用合同电、紧急电等去向变量后使用**闭合等式**。
2. 不采用“整体往返效率 90%”；最终：
   \[
   \eta_c=\eta_d=0.9,\qquad \eta_{rt}=0.81.
   \]
3. 不采用“Q3 理论分位约 0.91”作为正式参数；\((q,m)\) 只按融合口径的标定/敏感性解释。
4. 不采用 Q2/Q3/Q4 的粗略经验金额作为正式结果。

## 0.3 价格可见性主假设

题面只说“实时波动”，没有唯一裁定当天 144 槽价格是否在 0:00 全部可见。

### 主口径：Causal-price

时刻 \(t\) 只能使用：

\[
p^{real}_{d,\tau},\qquad \tau<t
\]

及当前合法发布的信息；未来槽价格由因果预测/场景给出。

这是对“实时波动”的保守解释，**不是题面明文唯一事实**。

### 解释敏感性：Known-day-price

若假设每天 0:00 已知当天全部 144 槽价格，则必须在该信息集下**完整重新优化 Q4-2/Q4-3**；禁止沿用 causal-price 的合同后只换结算价格。

定义：

\[
VOI_{day-price}
=
J^{cmp}_{causal}
-
J^{cmp}_{known-day}.
\]

它解释“整日价格提前可见”的信息价值，不解释算法优劣。

## 0.4 本轮最关键结构修复

### F1：叶路径不能直接代表多阶段节点

历史 analog path + k-medoids 并不会自动产生共享前缀树。正式 Q4-3 改为：

\[
\boxed{
\text{raw paired paths}
\rightarrow
\text{hard compatibility}
\rightarrow
\text{kernel weights}
\rightarrow
\text{stagewise tree construction}
\rightarrow
\text{node contract variables}
}
\]

合同变量按 node 建立：

\[
A^r_{n,t},
\]

场景叶通过：

\[
A_t^{r,\omega}
=
A^r_{n_r(\omega),t}
\]

调用，绝不允许完整叶身份提前决定早期合同。

### F2：点预测与随机场景必须原尺度均值一致

价格点预测使用 Duan smearing；价格场景不得再次重复 smearing。正式采用原尺度乘数归一化，保证 raw weighted scenario mean 与 point arm 一致。

### F3：Q3 历史接口消歧

Q4-3 正式路线唯一为：

\[
\boxed{
\text{tree-aware conditional SAA}
+
\text{node contracts}
+
\text{causal LDR}
+
\text{MPC}
+
\text{two-mode recourse}
}
\]

\((q,m)\) 与 E1 均不混入主 SAA。

### F4：continuation 必须 leaf/regime-consistent

主线允许 SOC-only pooled PWL，但同时把 regime-conditioned continuation 完整定义为可执行备选；是否升级在 1 月 prequential Gate 中冻结。

### F5：continuation LP certificate 只属于 LP value

PWL lower/upper certificate 只对连续 LP value 成立。进入 operational PWL 的 active knots 必须全部获得 exactness certificate，或 same-knot LP/MILP gap 小于冻结阈值；否则 formal run 失败。

---

# 1. 数据预处理

# 1.1 时间、单位、唯一主键

每日：

\[
t=0,\ldots,143,\qquad \Delta t=\frac16\text{ h}.
\]

槽：

\[
[t/6,(t+1)/6).
\]

统一键：

```text
(date, slot)
```

附件 3 另用：

```text
(issue_datetime, lead_hour, target_datetime)
```

必须检查：

1. 日期连续；
2. 每日 144 槽；
3. 无重复；
4. 无缺失/NaN/Inf；
5. 附件 2 与附件 4 `(date,slot)` 一一对齐；
6. 附件 3：
   \[
   target=issue+lead;
   \]
7. 首槽、6:00、12:00、18:00、末槽双向回读；
8. result 映射：
   \[
   \boxed{B[2+t]=x[t]}.
   \]

任何时间错位：

```text
FATAL_TIME_MAPPING
```

## 1.2 单位

负荷/PV 原始数据为 kW：

\[
L_{d,t}=\frac{P^{load}_{d,t}}6,
\qquad
P_{d,t}=\frac{P^{PV}_{d,t}}6.
\]

计划、调整、充电、放电、紧急电、弃光、未调用合同电均为 kWh/slot。

价格：

\[
p_{d,t}\text{ 元/kWh}.
\]

## 1.3 正式期与 January shadow pilot

正式：

\[
2025\text{-}02\text{-}01
\sim
2025\text{-}12\text{-}31
\]

共：

\[
334\times144=48096
\]

槽。

### January 协议

- 1/1–1/7：burn-in，只积累成熟价格/PV residual，不用于模型选优；
- 1/8–1/24：rolling/prequential selection；
- 1/25–1/31：完全不参与选择的 holdout；
- 每个候选使用独立 `shadow_chain_id`；
- 2/1 正式链统一重置：
  \[
  S_{2025-02-01,0}=6000;
  \]
- January shadow SOC 不传入正式期。

若 1/1 冷启动需要价格先验，仅允许使用已声明的固定先验（例如附件 1 既有固定电价）或“无评分 burn-in”，不得读取当天未来附件 4。

审计保存：

```text
decision_time
train_start
train_end
shadow_chain_id
candidate_id
holdout_flag
```

## 1.4 附件 4 数据画像 Gate

正式运行前实际计算并保存：

\[
p_{\min},p_{\max},p_{50},p_{1},p_{99},
\]

以及：

- 峰谷比；
- 日内极差；
- 近零价数量；
- \(N_{\le0}\)；
- lag-1 / lag-6 / lag-144；
- 价格与负荷、PV、净负荷的滚动相关。

这些数值必须从实际附件 4 读取，不能从历史审计稿抄写。

### Positive-price settlement Gate

若：

\[
N_{\le0}=0,
\]

可使用读法 C 的标准绝对值 epigraph。

若：

\[
p_{d,t}\le0
\]

出现，则：

```text
FATAL_PRICE_LINEARIZATION_ASSUMPTION
```

必须重新构造 piecewise/disjunctive settlement；不能继续依赖 \(Z\ge|G-A|\) 自动压紧。

---

# 2. 预测与随机输入层

# 2.1 Q4-2：冻结 Q2 PV 接口

Q4-2 不重新定义 Q2 的 \((q,m)\) 数学含义，而调用冻结接口：

\[
\boxed{
\widehat P^{Q2}_{d,t}
=
\mathcal P_{Q2}
\bigl(
\mathcal D_{<d};
q_0,m_0
\bigr)
}
\]

主标定点：

\[
(q_0,m_0)=(0.8,0),
\]

只称“标定点”，不称全局最优。

主分支必须逐位复现 FORMAL_Q2：

\[
\widetilde P_{d,t}
=
\operatorname{mean}
\{
P_{j,t}:j\in\text{最近4个完整成熟日}
\},
\]

\[
e_{j,t}
=
\frac{
P_{j,t}-\widetilde P_{j,t}
}{
\max(\widetilde P_{j,t},\varepsilon_{PV})
},
\]

\[
m^{Q20}_{d,t}
=
\operatorname{clip}
[-Q_{0.20}(e_{j,t}),0,0.35],
\]

\[
\widehat P^{Q2}_{d,t}
=
(1-m^{Q20}_{d,t})
\widetilde P_{d,t}
\]

作为默认参考实现。

Q4 不重新发明其他 q/m 公式；其他 \((q,m)\) arm 只调用冻结 Q2 标定函数，不在 Q4 中二次解释。

关闭 Q4 价格升级时：

\[
\boxed{
\widehat P^{Q4-2}
\equiv
\widehat P^{FORMAL-Q2}
}
\]

逐槽相等。

## 2.2 Q4-3：官方 PV forecast 重建

严格继承：

- issue：
  \[
  r\in\{0,6,12,18\};
  \]
- 第 \(k\) 小时：
  \[
  [r+k-1,r+k);
  \]
- 0:00 当天版直接覆盖 0–24 h；
- \(F_0\)：
  - 6/12/18：发布前刚结束的 10 min 实测 PV；
  - 0:00：D−1 23:50–24:00 实测 PV；
- 构造 \(F_0,\ldots,F_{24}\)；
- 小时内线性插值；
- 积分成 10 min 电量；
- 误差/修订只有 target mature 后入历史池；
- 主场景不含负荷误差。

## 2.3 价格 node point forecast

主口径 Causal-price 下，0/6/12/18 节点分别预测未来价格。

正价格时：

\[
y_{d,t}=\log p_{d,t}.
\]

### 同槽季节基线

\[
\widehat y^{base}_{d,t|0}
=
\frac{
\sum_{j<d}w_{d-j}y_{j,t}
}{
\sum_{j<d}w_{d-j}
},
\]

\[
w_h=2^{-h/H_p}.
\]

weekday/weekend shrinkage：

\[
\delta^{wd}_{g,t}
=
\alpha_{wd}
(
\bar y_{g,t}-\bar y_{\cdot,t}
),
\]

\[
\widehat y_{d,t|0}
=
\widehat y^{base}_{d,t|0}
+
\delta^{wd}_{g(d),t}.
\]

### 6/12/18 在线修正

仅使用：

\[
\tau<r.
\]

\[
u_{d,r}
=
\operatorname{HuberMean}
\{
y^{real}_{d,\tau}
-
\widehat y_{d,\tau|0}
\}.
\]

按近/中/远 lead-block：

\[
\widehat y_{d,t|r}
=
\widehat y_{d,t|0}
+
\gamma_{r,b(t-r)}u_{d,r}.
\]

### Hierarchical Duan smearing

\[
\widehat c_{r,b}
=
E[
\exp(\varepsilon^p)
\mid
r,b
].
\]

点预测：

\[
\boxed{
\widehat p^{mean}_{d,t|r}
=
\widehat c_{r,b(t-r)}
\exp(\widehat y_{d,t|r})
}
\]

小样本时：

```text
issue×lead-block
→ issue-only
→ global mature residual
```

逐级回退。

## 2.4 价格 rolling-origin residual

对历史 source day \(j\)：

\[
\widehat y^{replay}_{j,t|r}
=
f_p(
\mathcal D_{<j},
p^{real}_{j,<r}
),
\]

\[
\varepsilon^p_{j,t|r}
=
y_{j,t}
-
\widehat y^{replay}_{j,t|r}.
\]

必须：

\[
train\_end<decision\_time.
\]

禁止全样本拟合后回填历史 residual。

## 2.5 槽级 MPC price nowcast

合同场景/PWL 只在 0/6/12/18 更新；MPC 每槽允许用新增成熟价格前缀做轻量 nowcast。

最近合同节点：

\[
r(t)
=
\max\{
r\in\{0,36,72,108\}:r\le t
\}.
\]

\[
u^{mpc}_{d,t}
=
\operatorname{HuberMean}
\{
y^{real}_{d,\tau}
-
\widehat y_{d,\tau|r(t)}
:
t-h_{mpc}\le\tau<t
\}.
\]

\[
\widehat y^{mpc}_{d,\ell|t^-}
=
\widehat y_{d,\ell|r(t)}
+
\gamma^{mpc}_{b(\ell-t)}
u^{mpc}_{d,t}.
\]

原尺度：

\[
\boxed{
\widehat p^{mpc}_{d,\ell|t^-}
=
\widehat c^{mpc}_{b}
\exp(
\widehat y^{mpc}_{d,\ell|t^-}
)
}
\]

\(\widehat c^{mpc}\) 优先由成熟 nowcast residual 估计；样本不足时回退 node smearing。

nowcast 只供 MPC：

- 不反改合同；
- 不重算 node scenario tree；
- 不重算 node PWL；
- 不使用当前槽尚未揭示的 \(p_t\)。

## 2.6 Scaler 生命周期

采用**因果在线 robust scale**，不是 formal 期重新调参。

对每个特征维：

\[
scale_{d}
=
\max
\{
1.4826\,MAD(\mathcal H_{<d}),
scale_{floor}
\}.
\]

- 窗口长度/expanding 规则在 1 月冻结；
- \(scale_{floor}\) 在 1 月冻结；
- 正式期只允许按固定公式使用新增成熟历史更新 median/MAD；
- 记录：
  `scale_version, scale_train_end, median, MAD, floor`。

---

# 3. 目标日联合场景生成

# 3.1 Historical paired object

对每个成熟 target day \(j\)、节点 \(r\)，保存同源对象：

\[
\omega_j
\leftrightarrow
\left(
\varepsilon^{PV}_{j,\cdot|r},
\Delta F^{PV}_{j,\cdot},
\varepsilon^p_{j,\cdot|r}
\right).
\]

负荷在当前日主问题中 0:00 已知，因此当前日 SAA 不抽样负荷误差。

## 3.2 PV future-revision path

对未来官方发布节点 \(u>r\)，历史 revision：

\[
\Delta F^{PV,j}_{u\leftarrow parent(u),t}
=
F^{replay}_{j,t|u}
-
F^{replay}_{j,t|parent(u)}.
\]

目标日场景中的 hypothetical future forecast：

\[
F^{\omega}_{d,t|u}
=
\max\left\{
0,
F^{\omega}_{d,t|parent(u)}
+
\Delta F^{PV,j}_{u\leftarrow parent(u),t}
\right\}.
\]

当前节点锚定：

\[
F^\omega_{d,t|r}
=
F^{obs}_{d,t|r}.
\]

实际 PV 场景：

\[
\boxed{
P^\omega_{d,t}
=
\max\left\{
0,
F^\omega_{d,t|r_t}
+
\widetilde\varepsilon^{PV,\omega}_{t|r_t}
\right\}
}
\]

其中 \(r_t\) 是 t 之前最近发布节点。

若题面/附件给出可验证的光伏容量上界，再额外 clip 到容量；否则不人为发明 \(P^{cap}\)。

clip 引起的均值偏差必须报告，禁止暗中二次平移。

## 3.3 Price scenario：防 double-smearing

点预测已经包含 Duan smearing：

\[
\widehat p^{mean}_{t|r}
=
\widehat c_{r,b}
\exp(\widehat y_{t|r}).
\]

因此 raw price scenario **不再乘第二次 \(\widehat c\)**。

先对当前 raw compatible pool 的 log-residual 做中心化：

\[
\widetilde\varepsilon^{p,j}_{t|r}
=
\varepsilon^{p,j}_{t|r}
-
\bar\varepsilon^{p}_{t|r}.
\]

定义原尺度乘数归一化：

\[
\kappa_{t|r}
=
\sum_j
\pi^{raw}_{j|r}
\exp(
\widetilde\varepsilon^{p,j}_{t|r}
).
\]

正式 raw price path：

\[
\boxed{
p^j_{d,t|r}
=
\widehat p^{mean}_{d,t|r}
\frac{
\exp(\widetilde\varepsilon^{p,j}_{t|r})
}{
\kappa_{t|r}
}
}
\]

因此在 raw pool 上：

\[
\boxed{
\sum_j
\pi^{raw}_{j|r}
p^j_{d,t|r}
=
\widehat p^{mean}_{d,t|r}
}
\]

逐槽成立。

这保证 Q3-P0 → Q3-P1 只增加“价格随机性”，不偷偷改变均值路径。

## 3.4 Price future-node forecast in a scenario

价格没有官方 6/12/18 forecast revision 数据，因此未来 node price forecast 由**同一冻结预测器**作用于 scenario 已实现前缀：

\[
\widehat p^\omega_{\cdot|u}
=
f_p(
\text{history before }d,
p^\omega_{d,<u}
).
\]

这样未来节点的“可见价格信息”只由该场景到 u 为止的 realized prefix 产生，而不是直接读取场景未来尾部。

## 3.5 场景退化单测

若令：

\[
\varepsilon^{PV}=0,\quad
\Delta F^{PV}=0,\quad
\varepsilon^p=0,
\]

则所有场景必须退化为同一 point path：

\[
(P^\omega,p^\omega)
=
(\widehat P,\widehat p^{mean}).
\]

---

# 4. Tree-aware 场景树与条件概率

## 4.1 Hard compatibility

先做逻辑兼容，不做“连续值完全相等”。

历史 path 进入当前节点 raw pool 必须满足：

- 同 stage \(r\)；
- 所需 issue/target 完整；
- 当前之前所有 residual 已成熟；
- 无缺失；
- forecast reconstruction 口径相同；
- 若预注册 calendar hard-class，则 class 相同。

连续相似性全部留给 kernel，不用严格相等定义兼容。

记：

\[
\Omega^{hard}_{d,r}.
\]

## 4.2 Observable feature

节点 \(u\) 只用**截至 u 已经可观察的量**：

\[
o_u^\omega
=
\left(
\phi(\Delta F^{PV}_{\le u,\omega}),
\phi(\varepsilon^{PV}_{<u,\omega}),
\phi(\varepsilon^p_{<u,\omega}),
calendar/time
\right).
\]

\(\phi\) 使用低维块特征：

- 四个/当前已结束 6h block 均值；
- recent mean；
- cumulative mean；
- peak / valley；
- peak time；
- revision magnitude。

全部经 causal robust scale 标准化。

SOC 不进入概率特征。

## 4.3 Kernel weighting

先验：

\[
\pi_j^{prior}
\propto
w_j^{recency}
w_j^{calendar}
w_j^{season}.
\]

当前实际观测特征：

\[
o_r^{real}.
\]

\[
\widetilde\pi_j
=
\pi_j^{prior}
\exp\left[
-\frac{
\|o_r^{real}-o_r^j\|^2
}{
2h_r^2
}
\right].
\]

\[
\pi_j^{raw}
=
\frac{\widetilde\pi_j}
{\sum_{k\in\Omega^{hard}}\widetilde\pi_k}.
\]

## 4.4 Raw ESS

\[
\boxed{
ESS_r^{raw}
=
\frac1{
\sum_j(\pi_j^{raw})^2
}
}
\]

ESS 永远在场景缩减**之前**计算。

若低于冻结阈值：

1. 扩大预注册 bandwidth；
2. 取消最细软条件；
3. 在 hard-compatible 集内退到更粗季节/日型权重；
4. 最后在 hard-compatible 集内退到无条件 residual distribution。

不得复活 hard-incompatible path。

## 4.5 Stagewise tree construction

当前重优化节点为 root：

\[
\mathcal N_r=\{n_r^{root}\}.
\]

未来阶段：

\[
u\in\{6,12,18\},\quad u>r.
\]

在每个 parent node 内，仅用**到 u 为止的 observable feature**递归做 weighted clustering：

\[
n_u(\omega)
=
\mathcal Q_u
\bigl(
o_u^\omega
\mid parent=n_{parent(u)}(\omega)
\bigr).
\]

主候选 branching：

\[
2\times2\times2
\]

但：

- 总 leaf：
  \[
  K\le8;
  \]
- 每个非退化 child 至少 3 个 raw source days；
- 若样本不足，则 parent 不分裂；
- 聚类只使用当前 stage 已可观察 feature，禁止 full-path distance 决定早期 node。

## 4.6 Tree-aware reduction

普通 flat k-medoids 不能直接替代 stage tree。

正式顺序：

\[
\boxed{
hard\ compatibility
\rightarrow
kernel\ weighting
\rightarrow
ESS^{raw}
\rightarrow
stagewise\ node\ construction
\rightarrow
within\text{-}leaf\ medoid
}
\]

每个 terminal leaf 从其 raw members 中选一个 full-path medoid；leaf probability 为 cluster raw mass：

\[
\pi_\ell
=
\sum_{j\in cluster(\ell)}
\pi_j^{raw}.
\]

所有 parent probability 为 descendants mass 之和。

## 4.7 Moment / tail preservation

缩减后不做逐槽强制缩放，而检查预注册 block-level tolerance：

\[
gap^{price}_{b}
=
\frac{
|\bar p^{red}_b-\bar p^{raw}_b|
}{
\max(\bar p^{raw}_b,\epsilon)
},
\]

\[
gap^{PV}_{b}
=
\frac{
|\bar P^{red}_b-\bar P^{raw}_b|
}{
\max(\bar P^{raw}_b,\epsilon)
}.
\]

若超过阈值：

- 优先增加 leaf；
- 或重新选 medoid；
- 不对每个槽单独乘缩放因子。

## 4.8 Tail-stress 保留

快速 proxy：

\[
R_j^{proxy}
=
\sum_t
p^j_t
[L_t-P^j_t]^+.
\]

同时在代表日上计算 dispatch-aware stress：

\[
R_j^{dispatch}
=
J_j^{recourse}.
\]

缩减后至少保留一个来自 raw stress top-10% 的代表 leaf，并检查高风险 recourse 的决策保持。

## 4.9 Nonanticipativity 的真正实现

合同变量按 node 建：

\[
G_t^0
\]

只在 root 一份；

\[
A^u_{n,t},
\qquad n\in\mathcal N_u.
\]

叶调用：

\[
\boxed{
A_t^{u,\omega}
=
A^u_{n_u(\omega),t}
}
\]

因此共享 node 的场景天然共享合同，不再依赖“连续 history 恰好相等”。

---

# 5. Historical tail 与价格条件 continuation

## 5.1 同源完整日 block

future tail 的：

\[
(L^{tail},P^{tail},p^{tail})
\]

优先来自**同一个历史 source_date**，以保留：

\[
Corr(L,p),
\quad
Corr(P,p),
\quad
Corr(L-P,p).
\]

所有 source day 必须：

\[
source\_date<d.
\]

不逐槽拼接不同历史日。

## 5.2 Bellman 嵌套

当前日模型只负责：

\[
[r,B_d),
\qquad B_d=24{:}00.
\]

continuation 从：

\[
S_{B_d}
\]

开始。

设合法 forecast 覆盖至 \(T_1\)，historical tail 终点：

\[
T_2=T_1+W_V.
\]

\[
\boxed{
\Phi_{d,r}(s,\zeta)
=
\min
E
\left[
J_{B_d:T_1}^{forecast}
+
J_{T_1:T_2}^{hist}
+
V_{anchor}(S_{T_2})
\mid
S_{B_d}=s,\zeta
\right]
}
\]

其中 \(\zeta\) 表示 pooled 或 terminal regime。

远端 anchor：

\[
\boxed{
V_{anchor}(s)
=
0.4720(6000-s)
}
\]

只作 finite-horizon soft residual value，不设置年末硬 6000。

## 5.3 Pooled SOC-only continuation

最简单主候选：

\[
\Phi_0(s)
=
\Phi(s,\zeta=pooled).
\]

它是经过 1 月 state-sufficiency Gate 接受的近似，不声称 SOC 严格构成完整 Markov state。

## 5.4 Regime-conditioned continuation

为了使 audit 中的状态扩展可执行，定义日末 observable regime feature：

\[
\psi^\omega
=
\left(
\overline{\varepsilon^p}_{18:24,\omega},
\frac{\overline p_{18:24,\omega}}
{\overline p_{day,\omega}+\epsilon},
\overline{\varepsilon^{PV}}_{18:24,\omega}
\right).
\]

使用成熟历史做标准化后 weighted k-medoids：

\[
k(\omega)
=
\mathcal R(\psi^\omega),
\qquad
K_z\in\{1,2,3\}.
\]

\(K_z=1\) 即 pooled SOC-only。

对每个 regime：

\[
\Phi_k(s),\qquad k=1,\ldots,K_z.
\]

样本不足：

\[
n_k<n_{regime}^{min}
\]

则合并最近 regime；仍不足则回退 pooled \(K_z=1\)。

## 5.5 SAA leaf-terminal mapping

当前日每个 leaf 根据**该场景到日末已经会实现的变量**得到：

\[
k(\omega).
\]

终端 epigraph：

\[
z^{term,\omega}
\ge
a_{k(\omega),m}
S_{144}^{\omega}
+
b_{k(\omega),m},
\quad\forall m.
\]

因此 leaf 的 continuation 与其日末 regime 一致。

实际运行到日末后，用真实 \(\psi^{real}\) 选真实 regime；下一日重新构造新的 causal value。

## 5.6 MPC 使用的 terminal value

MPC 槽首尚不知道最终 regime，因此不使用未来 leaf identity。

在最近 node \(r(t)\) 上，根据当前 tree 的 terminal regime 概率：

\[
\Pr(k\mid n_{r(t)}^{real})
\]

构造 mixture continuation：

\[
\boxed{
\bar\Phi_{d,r(t)}(s)
=
\sum_{k=1}^{K_z}
\Pr(k\mid n_{r(t)}^{real})
\Phi_k(s)
}
\]

凸函数的概率加权仍为凸，可继续 PWL 表示。

这样：

- SAA leaf 用 regime-specific value；
- 实际 MPC 用 node-conditional mixture；
- 不提前知道未来 terminal regime。

---

# 6. PWL 构造与 exactness

## 6.1 定义域

对当前 node/tree 所有 compatible leaves 求日末 SOC 可达区间：

\[
\underline S^{PWL}
=
\min_\omega
\underline S^\omega,
\]

\[
\overline S^{PWL}
=
\max_\omega
\overline S^\omega.
\]

PWL 定义域：

\[
\boxed{
\mathcal R^{PWL}
=
[
\underline S^{PWL},
\overline S^{PWL}
]
}
\]

禁止域外外推。

## 6.2 初始 knots

5 个点：

1. lower endpoint；
2. 25%；
3. midpoint；
4. 75%；
5. upper endpoint。

## 6.3 Knot LP

对每个 regime \(k\) 和 knot \(s_j\)：

\[
v_{k,j}
=
\Phi_k^{LP}(s_j),
\]

\[
\mu_{k,j}
=
\text{初始 SOC 等式对偶}.
\]

先锁定经济最优值，再做 throughput/source-flow canonical cleanup，消除纯退化同充同放。

## 6.4 Active-knot exactness

对所有将进入 operational PWL 的 active knots：

\[
\chi^{cont}_{CD,k,j}
=
\max_{\omega,t}
\min(C^\omega_t,D^\omega_t).
\]

必须满足：

\[
\chi^{cont}_{CD,k,j}
\le\varepsilon_E,
\]

或 same-knot MILP 对照满足：

\[
0
\le
V^{MILP}_{k,j}
-
V^{LP}_{k,j}
\le
\varepsilon_{exact}.
\]

正式要求：

\[
\boxed{
exact\_active\_knots
=
active\_knots
}
\]

不能使用“大多数 knot 通过”措辞。

若任何 active knot gap 超阈值：

```text
CONTINUATION_EXACTNESS_FAIL
```

不得把该 PWL 写成已验证 physical value。

## 6.5 LP convex certificate

仅当 knot value 属于通过 exactness 的连续 LP surrogate 时：

\[
\mu_{k,j}\le\tau_\mu,
\]

且：

\[
\mu_{k,j+1}
-
\mu_{k,j}
\ge-\tau_\mu.
\]

支持下界：

\[
\Phi^L_k(s)
=
\max_j
\{
v_{k,j}
+
\mu_{k,j}(s-s_j)
\}.
\]

相邻 knots 弦线上界：

\[
\Phi^U_k(s).
\]

对 LP value：

\[
\Phi^L_k(s)
\le
\Phi^{LP}_k(s)
\le
\Phi^U_k(s).
\]

## 6.6 自适应误差

\[
R_{V,k}
=
\max_jv_{k,j}
-
\min_jv_{k,j}.
\]

\[
\varepsilon_{V,k}
=
\max
\{
\varepsilon_V^{abs},
\rho_VR_{V,k}
\}.
\]

若：

\[
g_{V,k}
=
\max_s
[
\Phi^U_k(s)-\Phi^L_k(s)
]
>
\varepsilon_{V,k},
\]

在最大 gap 点加 knot。

达到冻结 \(K_{max}\) 仍不通过：

```text
PWL_GAP_FAIL
```

## 6.7 Operational PWL

正式使用 upper secant：

\[
\boxed{
\widehat\Phi_k(s)
=
\Phi^U_k(s)
}
\]

弦线：

\[
a_{k,m}
=
\frac{
v_{k,m+1}-v_{k,m}
}{
s_{m+1}-s_m
},
\]

\[
b_{k,m}
=
v_{k,m}
-
a_{k,m}s_m.
\]

epigraph：

\[
z
\ge
a_{k,m}s+b_{k,m},
\quad\forall m.
\]

lower cuts 只作 certificate。


# 7. 共同物理模型与唯一 source-flow

## 7.1 交流侧变量

全部单位为 kWh/slot：

- \(U_t^L\)：普通合同电供负荷；
- \(U_t^C\)：普通合同电充电；
- \(W_t^G\)：已购但未调用普通电；
- \(P_t^L\)：PV 供负荷；
- \(P_t^C\)：PV 充电；
- \(R_t^{PV}\)：弃光；
- \(C_t\)：交流侧充电输入；
- \(D_t\)：交流侧放电输出；
- \(H_t\)：紧急购电；
- \(S_t\)：SOC。

效率只在 SOC 方程体现，不在 source-flow 中重复乘。

## 7.2 当前有效普通合同量

\[
Q_t^{contract}
=
\begin{cases}
G_t^{plan},&Q4\text{-}2,\\
A_t^F,&Q4\text{-}3.
\end{cases}
\]

## 7.3 Source-flow 等式

\[
Q_t^{contract}
=
U_t^L+U_t^C+W_t^G,
\]

\[
P_t
=
P_t^L+P_t^C+R_t^{PV},
\]

\[
U_t^L+P_t^L+D_t+H_t
=
L_t,
\]

\[
C_t
=
U_t^C+P_t^C.
\]

因此题面的“供给不少于负荷”被转化为带显式去向变量的**闭合等式**。

## 7.4 固定 source priority

真实执行固定：

1. PV 先供负荷；
2. 普通合同电补负荷；
3. 剩余 PV 优先充电；
4. 若仍需充电，普通合同电充电；
5. 剩余普通合同电记：
   \[
   W^G;
   \]
6. 剩余 PV 记：
   \[
   R^{PV}.
   \]

这只用于消除会计退化，不改变一级经济成本。

## 7.5 储能

\[
S^{min}=1200,\qquad
S^{max}=10800,
\]

\[
\bar E
=
5000/6
=
833.333333.
\]

\[
\eta_c=\eta_d=0.9.
\]

\[
\boxed{
S_{t+1}
=
S_t
+
0.9C_t
-
\frac{D_t}{0.9}
}
\]

\[
0\le C_t,D_t\le\bar E.
\]

实际执行：

\[
\boxed{
C_tD_t=0
}
\]

且：

\[
H_t\not\rightarrow C_t.
\]

未调用普通电：

\[
W_t^G
\]

不跨槽、不返售。

---

# 8. Q4-2：波动电价下重算 FORMAL_Q2

## 8.1 信息集

主口径 Causal-price：

\[
\mathcal I^{4-2}_{d,0}
=
\{
L_{d,0:143},
\widehat P^{Q2}_{d,0:143},
S^{exec}_{d,0},
\widehat p^{mean}_{d,0:143|0},
\mathcal H_{<d}
\}.
\]

未来：

\[
P^{real}_{d,t},
\quad
p^{real}_{d,t}
\]

不可见。

Known-day-price sensitivity 中仅把价格信息替换为：

\[
p^{real}_{d,0:143}
\]

并重新求解全天计划。

## 8.2 三个 price-value arm

### Q2-P0：冻结残值基线

\[
\lambda=0.4720.
\]

目标：

\[
\min
\sum_t
\widehat p_{d,t|0}^{mean}
G_t^{plan}
-
0.4720S_{d,144}^{plan}.
\]

### Q2-P1：正式默认候选——局部 Bellman marginal

不构造 full PWL；在预期日末 SOC 附近调用局部 continuation oracle。

### Q2-P2：完整 PWL

使用第 5–6 节的 full price-conditioned PWL，作为：

- January 结构选择 verifier；
- formal representative-day verifier；
- 灵敏度 alternative。

P1/P2 的正式主选择必须在 2 月前冻结。

## 8.3 P1 局部 Bellman oracle

### Step 1：P0 预求解

得到：

\[
s^{(0)}
=
S_{d,144}^{plan,(0)}.
\]

### Step 2：局部可达区间

以当前日末物理可达区间：

\[
\mathcal R_d^{local}
=
[
\underline S_d^{local},
\overline S_d^{local}
]
\]

为边界。

有限差分步长不再按全局 SOC 区间固定，而取：

\[
\delta_s^{(h)}
=
\rho_h
\left(
\overline S_d^{local}
-
\underline S_d^{local}
\right),
\]

\[
\rho_h
\in
\{0.005,0.01,0.02\}.
\]

靠近边界使用单边差分。

### Step 3：对偶与三步长有限差分

在：

\[
S_{B_d}=s^{(n)}
\]

求 local continuation LP：

\[
V(s^{(n)}),
\]

读取：

\[
\mu^{dual}(s^{(n)}).
\]

对三个 \(\delta_s^{(h)}\) 求有限差分：

\[
\mu_h^{fd}
=
\frac{
V(s+\delta_h)-V(s-\delta_h)
}{
2\delta_h
}.
\]

若三步长 slope dispersion 小于冻结阈值，且 dual 与 median FD 一致，则：

\[
\mu^{local}
=
\mu^{dual}.
\]

否则：

\[
\mu^{local}
=
\operatorname{median}
\{
\mu_h^{fd}
\}
\]

并记录：

```text
LOCAL_DUAL_UNSTABLE
```

### Step 4：更新边际价值

\[
\boxed{
\lambda_d^{(n+1)}
=
-\mu^{local}(s^{(n)})
\ge0
}
\]

重求日 LP。

### Step 5：停止

\[
|\lambda_d^{(n+1)}-\lambda_d^{(n)}|
\le
\varepsilon_\lambda
\]

或连续两次 \(S_{144}\) 落入相同 local segment。

最大迭代：

\[
N_{max}=5.
\]

若失败，使用**预注册单次 fallback slope**，不得称为“稳定 slope”，并必须报告相对 P2 regret。

## 8.4 局部仿射 value

以最终参考 SOC：

\[
s_d^{ref}
\]

定义：

\[
\beta_d^{(4)}
=
V(s_d^{ref})
+
\lambda_d^{(4)}s_d^{ref}.
\]

\[
\widetilde V_d^{(4)}(s)
=
\beta_d^{(4)}
-
\lambda_d^{(4)}s.
\]

优化时 \(\beta_d^{(4)}\) 可省略，但审计必须保留。

## 8.5 日前计划 LP

变量：

\[
G_t^{plan},
C_t^{plan},
D_t^{plan},
R_t^{plan},
S_t^{plan}.
\]

P1 一级目标：

\[
\boxed{
\min
\sum_{t=0}^{143}
\widehat p^{mean}_{d,t|0}
G_t^{plan}
-
\lambda_d^{(4)}
S_{d,144}^{plan}
}
\]

约束：

\[
G_t^{plan}
+
\widehat P_t^{Q2}
+
D_t^{plan}
=
L_t
+
C_t^{plan}
+
R_t^{plan},
\]

\[
0\le R_t^{plan}
\le
\widehat P_t^{Q2},
\]

\[
S_{t+1}^{plan}
=
S_t^{plan}
+
0.9C_t^{plan}
-
D_t^{plan}/0.9,
\]

\[
S_{d,0}^{plan}
=
S_{d,0}^{exec}.
\]

不设日末或年末硬 6000。

### P2 目标

若 January 冻结为 P2：

\[
z_d
\ge
a_mS_{144}^{plan}+b_m,
\quad
\forall m,
\]

\[
\min
\sum_t
\widehat p^{mean}_{d,t|0}G_t^{plan}
+
z_d.
\]

P1 与 P2 terminal 不得重复叠加。

## 8.6 计划层词典序与 exactness

一级得到：

\[
J_1^*.
\]

锁定：

\[
J_1
\le
J_1^*
+
\varepsilon_{lex}.
\]

二级：

\[
\min
\sum_t
(C_t^{plan}+D_t^{plan}).
\]

再扫描：

\[
\chi_{CD}^{plan}
=
\max_t
\min(C_t^{plan},D_t^{plan}).
\]

若超阈值，执行 active-set MILP closure。

只有通过 exactness 后才冻结：

\[
G^{plan}.
\]

## 8.7 透明执行器

真实运行逐槽：

\[
g_t
=
L_t
-
P_t^{real}
-
G_t^{plan}.
\]

若：

\[
g_t>0,
\]

则：

\[
C_t^{exec}=0,
\]

\[
D_t^{exec}
=
\min
\{
g_t,
\bar E,
0.9(S_t-1200)
\},
\]

\[
H_t
=
\max
\{
g_t-D_t^{exec},0
\}.
\]

若：

\[
g_t\le0,
\]

则：

\[
D_t^{exec}=H_t=0,
\]

\[
C_t^{exec}
=
\min
\{
-g_t,
\bar E,
(10800-S_t)/0.9
\}.
\]

source-flow 按第 7.4 节拆分。

### 实时价格不改变 Q4-2 executor

正式 Q4-2 中：

\[
p_t^{real}
\]

只用于：

1. 当前现金结算；
2. 后续 mature price history；
3. 后续日 price-conditioned value。

它不改变本槽透明物理动作，避免 Q4-2 偷换成 Q3-style MPC。

## 8.8 Q4-2 现金费用与比较指标

\[
\boxed{
J_{cash}^{4-2}
=
\sum_{d,t}
p_{d,t}^{real}
G_{d,t}^{plan}
+
\sum_{d,t}
5p_{d,t}^{real}
H_{d,t}
}
\]

普通购电按计划量收费，不按实际调用量。

\[
\boxed{
J_{cmp}^{4-2}
=
J_{cash}^{4-2}
+
0.4720
(6000-S_{end}^{4-2})
}
\]

0.4720 在这里是统一 terminal normalization，不等于 Q4 的动态局部库存价值。

---

# 9. Q4-3：Tree-aware conditional SAA + MPC

## 9.1 合同层

0:00：

\[
G_t^0.
\]

未来 stage：

\[
u\in\{6,12,18\}.
\]

每个 stage-node：

\[
A^u_{n,t},
\qquad
n\in\mathcal N_u.
\]

对场景 leaf \(\omega\)：

\[
A_t^{u,\omega}
=
A^u_{n_u(\omega),t}.
\]

实际路径最终合同：

\[
A_{d,t}^{F,real}
=
\begin{cases}
G_{d,t}^0,&0\le t<36,\\
A^{6}_{n_6^{real},t},&36\le t<72,\\
A^{12}_{n_{12}^{real},t},&72\le t<108,\\
A^{18}_{n_{18}^{real},t},&108\le t<144.
\end{cases}
\]

未来 stage 可以重新优化所有尚未执行槽，但最终实际结算只取该槽执行前最后一次生效合同。

## 9.2 主结算：读法 C

\[
\boxed{
C_C(G,A;p)
=
p\min(G,A)
+
0.5p(G-A)^+
+
1.5p(A-G)^+
}
\]

等价：

\[
C_C(G,A;p)
=
pA
+
0.5p|G-A|.
\]

正价格条件下，令：

\[
Z_t^\omega
\ge
G_t^0-A_t^{F,\omega},
\]

\[
Z_t^\omega
\ge
A_t^{F,\omega}-G_t^0,
\]

\[
Z_t^\omega\ge0,
\]

则最优自动满足：

\[
Z_t^\omega
=
|G_t^0-A_t^{F,\omega}|.
\]

中间 \(A^6/A^{12}/A^{18}\) 不重复收费。

## 9.3 规划物理 recourse 与 LDR

合同 \(A\) 不进入 LDR，由 node 变量决定。

参考动作：

\[
x_t^{ref}
=
(
C_t^{ref},
D_t^{ref},
H_t^{ref},
R_t^{PV,ref},
U_t^{L,ref},
U_t^{C,ref},
P_t^{L,ref},
P_t^{C,ref}
).
\]

\[
x_t^{ref,\omega}
=
x_t^0
+
\sum_{\tau<t}
B_{t\tau}^{PV}
\xi_{\tau}^{PV,\omega}
+
\sum_{\tau<t}
B_{t\tau}^{p}
\xi_{\tau}^{p,\omega}.
\]

严格：

\[
B_{t\tau}^{PV}
=
B_{t\tau}^{p}
=
0,
\qquad
\tau\ge t.
\]

因此 LDR 只能使用过去已经实现的扰动。

## 9.4 SAA source-flow

对每个 leaf \(\omega\)：

\[
A_t^{F,\omega}
=
U_t^{L,\omega}
+
U_t^{C,\omega}
+
W_t^{G,\omega},
\]

\[
P_t^\omega
=
P_t^{L,\omega}
+
P_t^{C,\omega}
+
R_t^{PV,\omega},
\]

\[
U_t^{L,\omega}
+
P_t^{L,\omega}
+
D_t^\omega
+
H_t^\omega
=
L_t,
\]

\[
C_t^\omega
=
U_t^{C,\omega}
+
P_t^{C,\omega},
\]

\[
S_{t+1}^\omega
=
S_t^\omega
+
0.9C_t^\omega
-
D_t^\omega/0.9.
\]

## 9.5 Node/leaf-consistent SAA objective

若采用 regime-conditioned continuation，则每个 leaf 用：

\[
k(\omega).
\]

引入：

\[
z^{term,\omega}
\ge
a_{k(\omega),m}
S_{144}^\omega
+
b_{k(\omega),m},
\quad\forall m.
\]

在当前 root \(r\)：

\[
\boxed{
\min
\sum_{\omega\in\Omega_r^{leaf}}
\pi_\omega
\left[
\sum_{t=r}^{143}
\left(
p_t^\omega
A_t^{F,\omega}
+
0.5p_t^\omega
Z_t^\omega
+
5p_t^\omega
H_t^\omega
\right)
+
z^{term,\omega}
\right]
}
\]

若 \(K_z=1\)，自然退化为 pooled SOC-only PWL。

过去已发生费用是 sunk cost，不重复进入当前重优化。

## 9.6 SAA 三级 canonical cleanup

### Level 1：经济成本

求：

\[
J_{SAA,1}^*.
\]

### Level 2：LDR/reference 偏差

锁定：

\[
J_{SAA,1}
\le
J_{SAA,1}^*
+
\varepsilon_{SAA,lex1}
\]

后最小化：

\[
\sum_{\omega,t}
\pi_\omega
\|x_t^\omega-x_t^{ref,\omega}\|_1.
\]

得到：

\[
J_{SAA,2}^*.
\]

### Level 3：source-flow/throughput cleanup

再锁定 Level 2，在其最优面内最小化：

\[
\sum_{\omega,t}
\pi_\omega
\left(
C_t^\omega
+
D_t^\omega
+
W_t^{G,\omega}
+
R_t^{PV,\omega}
\right).
\]

这样规划参考路径唯一化，减少 SAA 会计退化。

## 9.7 Planning exactness 与 conflict graph

Planning/SAA exactness 与 continuation exactness 分开。

对 SAA：

\[
\mathcal V
=
\{
(\omega,t):
\min(C_t^\omega,D_t^\omega)
>
\varepsilon_E
\}.
\]

若非空，建立 conflict graph：

- vertex：\((\omega,t)\) 充/放模式单元；
- 若两个 vertex 通过同一 node action、nonanticipativity 或共享 LDR coefficient 耦合，则连边；
- active-set closure 取违规 vertex 所在的连通分量；
- 对整个 closure 一致引入 mode binary；
- 全局重求并重新扫描。

迭代直到：

\[
\mathcal V=\varnothing.
\]

MILP 后必须再次检查：

- node contract equality；
- LDR 下三角；
- shared-node 决策一致。

## 9.8 0/6/12/18 实际条件更新

每个实际节点 \(r\)：

1. 冻结过去合同、物理动作、费用、SOC；
2. 读取最新官方 PV forecast；
3. 读取已成熟真实 PV/price prefix；
4. 更新 node price point forecast；
5. hard-compatible filtering；
6. kernel weighting；
7. raw ESS；
8. stagewise tree-aware reduction；
9. 更新 continuation / PWL；
10. 求当前 root conditional SAA；
11. 当前 \(A^r\) 对所有未来 compatible descendants 共享；
12. 只更新未来尚未执行槽。

## 9.9 10 min MPC

### Filtration

槽首：

\[
\mathcal F_{t^-}
=
\sigma(
S_t,
P^{real}_{<t},
p^{real}_{<t},
latest\ forecasts,
current\ contract,
node\ PWL
).
\]

\[
x_t^{ref}
\in
\mathcal F_{t^-}.
\]

当前槽真实 PV/price 出现后：

\[
\mathcal F_t
=
\mathcal F_{t^-}
\vee
\sigma(P_t^{real},p_t^{real}).
\]

### Horizon

主设置：

\[
\mathcal H_t
=
\{t,t+1,\ldots,143\}.
\]

未来 PV 使用最新合法 official point path；未来 price 使用：

\[
\widehat p^{mpc}_{\ell|t^-}.
\]

terminal 使用第 5.6 节 node-conditional mixture PWL：

\[
\bar\Phi_{d,r(t)}(S_{144}).
\]

### MPC 一级目标

\[
\boxed{
\min
\sum_{\ell=t}^{143}
5\widehat p_{\ell|t^-}^{mpc}
H_\ell
+
\bar\Phi_{d,r(t)}(S_{144})
}
\]

得到：

\[
J_{MPC,1}^*.
\]

### MPC 二级目标

锁定一级：

\[
J_{MPC,1}
\le
J_{MPC,1}^*
+
\varepsilon_{MPC,lex1}
\]

后最小化：

\[
\sum_{\ell=t}^{143}
\|x_\ell-x_\ell^{LDR}\|_1.
\]

### MPC 三级 cleanup

在前两级最优面内：

\[
\min
\sum_{\ell=t}^{143}
(
C_\ell
+
D_\ell
+
W_\ell^G
).
\]

只执行第一槽参考动作。

## 9.10 MPC time-limit 与 fallback

1 月冻结：

- `mpc_time_limit`；
- acceptable solver statuses；
- warm-start 方式；
- `max_plan_age`；
- reduced-fallback 配置。

正式运行：

1. 正常优先求完整 day-end MPC；
2. 若 time limit，无合格新解，则沿用最近有效 MPC plan 的下一步；
3. 记录：
   \[
   plan\_age;
   \]
4. 若：
   \[
   plan\_age>max\_plan\_age,
   \]
   切换预注册 reduced deterministic MPC；
5. reduced MPC 仍失败：
   ```text
   FATAL_MPC_SOLVE
   ```

two-mode recourse 仍必须保证当前槽物理可行。

## 9.11 当前槽 two-mode physical recourse

当前槽：

\[
P_t^{real},p_t^{real}
\]

揭示后，固定当前：

\[
A_t^{F,real}
\]

与槽首 reference。

### Charge mode

\[
D_t=H_t=0.
\]

### Supply mode

\[
C_t=0.
\]

词典序：

1. 最小真实应急费用：
   \[
   5p_t^{real}H_t;
   \]
2. 最小偏离槽首 reference；
3. 固定 source priority cleanup。

取两个模式中词典序更优的可行解。

因此实际：

\[
C_t^{exec}D_t^{exec}=0.
\]

## 9.12 Q4-3 现金费用

\[
\boxed{
J_{cash}^{4-3}
=
\sum_{d,t}
C_C(
G_{d,t}^0,
A_{d,t}^{F,real};
p_{d,t}^{real}
)
+
\sum_{d,t}
5p_{d,t}^{real}
H_{d,t}^{real}
}
\]

## 9.13 Q4-3 比较指标

\[
\boxed{
J_{cmp}^{4-3}
=
J_{cash}^{4-3}
+
0.4720
(6000-S_{end}^{4-3})
}
\]

同样只作跨方案 terminal normalization。

## 9.14 Planning surrogate 与真实策略差

\[
\Delta_{policy}
=
J_{real}^{MPC}
-
\widehat J_{SAA}^{LDR}.
\]

同时报告：

- expected/realized cost；
- H；
- S_end；
- contract；
- SOC path；
- scenario cost quantiles。

---

# 10. 求解组织

## 10.1 主求解器

\[
\boxed{
\text{HiGHS sparse LP/SAA}
}
\]

必要时：

\[
\boxed{
\text{iterative active-set MILP}
}
\]

不使用 GA/PSO/NSGA-II 搜 144 维调度轨迹。

## 10.2 正确的 Q4-3 求解顺序

```text
raw paired paths
→ hard compatibility
→ kernel weighting
→ ESS_raw
→ stagewise tree construction
→ tree-aware reduction
→ stage/node/leaf index
→ node G/A variables
→ leaf physical recourse
→ node mapping + causal LDR
→ regime/pooled continuation
→ SAA
→ active-set exactification
```

## 10.3 Q4-2 日循环

```text
real S_start
→ frozen Q2 PV forecast
→ node price point forecast
→ local Bellman oracle (or frozen P2)
→ daily LP
→ lexicographic cleanup
→ planning exactness
→ freeze Gplan
→ transparent causal execution
→ actual-price settlement
→ independent validation
→ carry real S_end
```

## 10.4 Q4-3 日循环

```text
0:00:
  official PV
  + causal price forecast
  → raw paired scenarios
  → tree
  → continuation
  → root SAA
  → freeze G0

each slot:
  MPC price nowcast
  → MPC reference
  → reveal current PV/price
  → two-mode recourse
  → update real SOC

6/12/18:
  newest official PV
  + matured real prefixes
  → rebuild current-root tree
  → continuation
  → conditional SAA
  → update future A only
```

## 10.5 并行边界

可并行：

- 同一 regime 的不同 continuation knots；
- 不同 regime 的 PWL；
- scenario distance / clustering candidate；
- independent validators；
- pre-registered sensitivity arms。

不可并行：

\[
d=1\rightarrow2\rightarrow\cdots\rightarrow334
\]

的真实 SOC 主链。

## 10.6 January 分阶段选择，防多重比较

禁止把所有候选做大规模笛卡尔积后在 24 天上挑最低费用。

### Phase A：预测层

只筛：

- calibration；
- peak timing；
- rolling MAE/RMSE；
- leakage。

淘汰明显不合格模型。

### Phase B：场景层

只比较少数预注册组合：

- bandwidth；
- ESS；
- leaf count；
- tree granularity。

### Phase C：决策层

使用 rolling folds 比较：

- P1/P2；
- pooled/regime continuation；
- MPC frequency。

### Phase D：holdout

1/25–1/31 不参与选择，只做一次最终 shadow validation。

### Complexity tie-break

若候选费用差在 one-standard-error / 冻结容差内，选择：

\[
\boxed{
\text{更简单、更快、更易审计}
}
\]

的模型。

## 10.7 计算预算记录

### Q4-2

- local continuation LP count；
- finite-difference LP count；
- daily LP runtime；
- P2 knot LP runtime；
- MILP trigger rate。

### Q4-3

- hard-compatible count；
- ESS_raw；
- node count；
- leaf count；
- tree runtime；
- PWL runtime；
- SAA runtime；
- MPC count/runtime；
- micro-LP runtime；
- MILP closure size/rate；
- plan age。

报告 median / P90 / P95 / max / peak memory / estimated 334-day runtime。

## 10.8 计算降级顺序

优先：

1. leaf：
   \[
   8\rightarrow6;
   \]
2. 压缩 LDR feature / shared coefficient；
3. tail：
   \[
   7\rightarrow5\rightarrow3\text{ days};
   \]
4. MPC 重优化频率：
   \[
   10\rightarrow20\rightarrow30\text{ min};
   \]
5. 在仍满足：
   \[
   g_V\le\varepsilon_V
   \]
   的前提下降低 \(K_{max}\)。

禁止通过：

- 删除 6/12/18 official nodes；
- 允许 leaf-perfect-information；
- 降低 active-knot exactness；
- 接受 unresolved C/D；
- future leakage；

来省算力。


# 11. 验证体系

> **重要**：本章写的是“必须通过的验证”，不是“已经通过”。  
> 状态统一为：
>
> ```text
> specification-fixed
> implementation-pending
> numeric-validation-pending
> verified
> ```

# 11.1 Static semantics Gate

检查：

- Q4-2/Q4-3 正式期均为 334 天；
- \(S_{start}=6000\)；
- 无年度硬回 6000；
- \(\eta_c=\eta_d=0.9\)，round-trip=0.81；
- 普通购电为供给正项；
- no sell；
- 读法 C 唯一主命名；
- Q4-2 不使用附件 3；
- Q4-3 不把 Q2 自建 PV predictor 偷渡进主线；
- `QM_ROLE_Q43=BASELINE_ONLY`；
- `FORMAL_Q3_EXECUTOR=MPC_TWO_MODE`；
- E1 仅 legacy；
- result mapping = `B[2+t]`。

## 11.2 Data / Unit / Mapping Gate

要求：

```text
raw_price_days          = 365
formal_days             = 334
slots_per_day           = 144
duplicate_keys          = 0
NaN_Inf                 = 0
attachment2_4_alignment = PASS
forecast_key_integrity  = PASS
mapping_errors          = 0
```

量纲单测：

\[
kW\times\frac16 h
=
kWh/slot.
\]

## 11.3 Cold-start / shadow Gate

验证：

- 1/1–1/7 不进入 model-selection score；
- 1/25–1/31 holdout 从未参与参数选择；
- 每个 candidate 有独立 shadow SOC chain；
- 2/1 所有 formal arms：
  \[
  S_0=6000;
  \]
- January future attachment 4 price 不泄漏到 earlier decision。

## 11.4 Price-visibility Gate

### Causal-price

任意扰动：

\[
p_{d,\tau}^{real},\qquad \tau\ge t
\]

不得改变 \(t^-\) 之前形成的：

- price forecast；
- scenario tree；
- node contract；
- PWL；
- MPC reference。

### Known-day-price

必须从 0:00 在“全天价格已知”的信息集下完整重优化，禁止只换结算价。

## 11.5 Q2 frozen-interface regression

关闭 Q4 price-value upgrade，恢复原 Q2 price input 后：

\[
\boxed{
Q4\text{-}2
\rightarrow
FORMAL\_Q2
}
\]

至少逐位核：

- \(\widehat P^{Q2}\)；
- \(G^{plan}\)；
- transparent execution；
- \(H\)；
- SOC；
- \(J_{cash}\)。

## 11.6 Q3 inheritance Gate

配置断言：

```text
formal_q3_executor = MPC_TWO_MODE
legacy_q3_executor = E1
qm_role_q43        = BASELINE_ONLY
```

关闭 Q4 price uncertainty 并恢复固定价格后：

\[
\boxed{
Q4\text{-}3
\rightarrow
FORMAL\_Q3
}
\]

另可独立 replay 历史 \((q,m)=(0.8,0)\) 标定点，但不得与 formal Q4-3 主 SAA 混为一条结果。

## 11.7 Scenario generation Gate

### Price

检查 raw pool：

\[
\left|
\sum_j
\pi_j^{raw}
p^j_{t|r}
-
\widehat p^{mean}_{t|r}
\right|
\le\tau_{mean,p}.
\]

确认 Duan smearing 应用次数：

```text
smearing_applied_count = 1
```

### PV

检查：

- current forecast anchor；
- future revision 累积；
- residual maturity；
- nonnegative clipping；
- clip 前后 mean gap。

### Degenerate residual test

残差置零后：

\[
P^\omega=\widehat P,
\quad
p^\omega=\widehat p^{mean}
\]

对所有场景成立。

## 11.8 Tree / Nonanticipativity Gate

必须：

1. 当前 root node 数：
   \[
   1;
   \]
2. 每 stage 保存：
   `node_id,parent_id,node_leaf_count`；
3. 同 node 的合同：
   \[
   A^u_{n,t}
   \]
   只有一份；
4. 两条“前缀相同、尾部不同”的人工路径，其分叉前合同逐位相同；
5. 随机置换分叉后未来数据，分叉前合同不变；
6. leaf-independent 错误模型只允许作 hidden-clairvoyance lower benchmark，不进入主结果。

## 11.9 Hard compatibility / Kernel / ESS Gate

分别报告：

```text
hard_compatible_count
kernel_bandwidth
ESS_raw
fallback_level
```

验证：

- hard-incompatible weight 永远为 0；
- ESS 在 reduction 前；
- 扩 bandwidth 不等于“复活旧分支”；
- SOC 不进入概率特征。

## 11.10 Tree reduction Gate

缩减前后检查：

- price/PV block mean；
- variance；
- P10/P90；
- price autocorrelation；
- PV revision correlation；
- PV-price correlation；
- dispatch stress tail；
- parent/child probability consistency；
- \(G/A/H/SOC/J\) 的代表日 decision preservation。

## 11.11 Scaler Gate

审计：

```text
scale_version
scale_train_end
median
MAD
MAD_floor
```

必须：

\[
scale\_train\_end<decision\_time.
\]

正式期可因果更新 scale，但算法、窗口、floor 不得改变。

## 11.12 Historical-tail Gate

检查每个 tail block：

\[
source\_date<d.
\]

优先验证同 source day：

\[
(L,P,p).
\]

比较 raw history 与 tail pool 的：

\[
Corr(L,p),
\quad
Corr(P,p),
\quad
Corr(L-P,p).
\]

current-day 与 tail 时间域不得重叠、不得断档。

## 11.13 PWL Domain Gate

所有 SAA leaf：

\[
S_{144}^\omega
\in
[
S_{min}^{PWL},
S_{max}^{PWL}
].
\]

域外：

```text
FATAL_PWL_EXTRAPOLATION
```

必须扩展定义域并重求，不允许线性外推。

## 11.14 Active-knot Exactness Gate

要求：

\[
exact\_active\_knots
=
active\_knots.
\]

对每个 active knot：

\[
\chi^{cont}_{CD}\le\varepsilon_E
\]

或：

\[
0
\le
V^{MILP}-V^{LP}
\le
\varepsilon_{exact}.
\]

报告：

```text
active_knot_count
exact_knot_count
max_exactification_gap
```

## 11.15 PWL Convexity / Gap Gate

\[
\mu_j\le\tau_\mu,
\]

\[
\mu_{j+1}-\mu_j\ge-\tau_\mu,
\]

\[
\Phi^L(s)
\le
\Phi^U(s)+\tau_V,
\]

\[
g_V
\le
\varepsilon_V.
\]

记录：

```text
PWL_ROLE = UPPER_SECANT
```

## 11.16 Regime-continuation Gate

若 January 冻结 \(K_z>1\)：

- regime feature 只用 day-end 可观察变量；
- \(K_z\in\{2,3\}\)；
- 每 regime 样本量达标；
- leaf-to-regime map 唯一；
- SAA leaf 使用 \(\Phi_{k(\omega)}\)；
- MPC 使用 node mixture，不使用未来 leaf id；
- out-of-fold continuation error 与下游 \(J_{cmp}\) 相对 pooled 有实质改善。

若不满足，正式主线冻结：

\[
K_z=1.
\]

## 11.17 SAA Epigraph Gate

逐 leaf：

\[
z^{term,\omega}
=
\max_m
\left[
a_{k(\omega),m}
S_{144}^\omega
+
b_{k(\omega),m}
\right]
\]

在数值容差内成立。

## 11.18 LDR Causality Gate

\[
|B_{t\tau}|
\le
\tau_B,
\qquad
\tau\ge t.
\]

active-set MILP 后再次验证 lower triangular structure 不被破坏。

## 11.19 SAA / MILP Conflict-closure Gate

检查：

- conflict graph 连通分量；
- shared node action；
- shared LDR coefficient；
- closure size；
- binary count；
- MILP 后重新全局扫描。

要求：

\[
\max_{\omega,t}\min(C_t^\omega,D_t^\omega)
\le
\varepsilon_E.
\]

## 11.20 Q4-2 Local-value Gate

报告：

- dual slope；
- 三个 finite-difference slopes；
- slope dispersion；
- iteration count；
- fallback count；
- \(P1/P2\) regret。

January 结构选择用：

\[
Regret_{P1|P2}
=
J_{cmp}^{P1}
-
J_{cmp}^{P2}.
\]

formal representative-day 的 P2 只作 verifier，不再用于改主模型。

## 11.21 MPC Gate

每槽保存：

```text
MPC_terminal_node
MPC_PWL_version
MPC_price_nowcast_version
MPC_plan_age
solver_status
```

验证：

- terminal 来自 \(r(t)\) 当前 node mixture；
- nowcast 只用 \(<t\) price；
- intra-block nowcast 不反改合同；
- `plan_age` 不超过冻结上限；
- fallback 触发次数可审计。

## 11.22 Current-slot Filtration Gate

人工扰动当前槽真实 PV/price：

- \(\mathcal F_{t^-}\) reference 不变；
- \(\mathcal F_t\) two-mode recourse 可以改变；
- 下一槽 SOC 可以改变。

## 11.23 Settlement Gate

黄金样例：

\[
G=100,\ A=80
\Rightarrow
90p,
\]

\[
G=100,\ A=120
\Rightarrow
130p.
\]

并验证：

\[
Z=|G-A|.
\]

禁止：

- 中间 A 重复收费；
- 6h 块先净额抵消；
- 预测价作为最终结算价；
- Q4-2 普通电按实际调用量计费。

## 11.24 Physical Gate

独立程序逐槽重算：

\[
1200
\le
S_t
\le
10800,
\]

\[
0
\le
C_t,D_t
\le
833.333333,
\]

\[
C_tD_t=0.
\]

检查：

- load satisfaction；
- no selling；
- H 不充电；
- W^G 不跨槽；
- curtailment 不超 PV；
- SOC 跨日连续。

## 11.25 Policy-gap Gate

\[
\Delta_{policy}
=
J_{real}^{MPC}
-
\widehat J_{SAA}^{LDR}.
\]

同时报告：

- expected vs realized；
- H；
- SOC；
- contract；
- tail cost quantiles。

过大时只能回到 January model-design 阶段，不得 formal 期用未来数据修补。

## 11.26 Price-information Lower Benchmarks

区分：

### LB-day-price

每天 0:00 知道当天 144 槽真实 price；PV 信息保持正式口径。

### LB-horizon-price

知道当前 optimization horizon 内未来 price；PV 信息保持正式口径。

### LB-global-price

知道 334 天全部 future price，但 PV 仍按正式信息结构。

它们是**价格信息下界**，不是完整 PV+price clairvoyant lower bound。

同机制、同 terminal normalization 下应满足：

\[
J_{LB-global-price}
\le
J_{LB-horizon-price}
\le
J_{LB-day-price}
\le
J_{formal}.
\]

若不满足，先查信息集和边界口径。

## 11.27 Formal Parameter-hash Gate

2/1 前冻结：

```text
forecast_config_hash
scenario_config_hash
tree_config_hash
PWL_config_hash
MPC_config_hash
MILP_config_hash
qm_role
formal_q3_executor
```

2–12 月这些结构 hash 不得变化。

## 11.28 Result Readback Gate

从最终：

- result4-2.xlsx；
- result4-3.xlsx；

重新读取并核：

- 映射；
- 单位；
- 计划/调整；
- SOC；
- 费用；
- 紧急区间；
- 连续区间合并。

要求：

```text
readback_errors = 0
```

---

# 12. 公平基线与价值归因

## 12.1 Q4-2

| Arm | 变化 | 解释 |
|---|---|---|
| Q2-P0 | causal price point + \(\lambda^*=0.4720\) | price forecast 基线 |
| Q2-P1 | P0 + local Bellman marginal | **正式默认候选** |
| Q2-P2 | full price-conditioned PWL | verifier / alternative |
| LB-day-price | 全天价 0:00 已知 | same-day price VOI |
| LB-global-price | 全年 price 已知 | price-only theoretical lower bound |

## 12.2 Q4-3

| Arm | 变化 | 解释 |
|---|---|---|
| Q3-P0 | PV-only tree + deterministic mean price + PV-only Q3 PWL | baseline |
| Q3-P1 | P0 + joint PV-price tree；均值固定 | price uncertainty value |
| Q3-P2 | P1 + price-conditioned continuation | **正式主候选** |
| Q3-P2R | P2 + regime-conditioned continuation | state-dimension sensitivity |
| LB-day-price | 当日价格 0:00 已知 | price VOI |

`Q3-P0→P1` 必须保持 mean price path 一致，因此差值才能解释为“随机价格分布信息价值”。

## 12.3 更新时点价值

\[
N_0=\{0\},
\]

\[
N_1=\{0,6\},
\]

\[
N_2=\{0,6,12\},
\]

\[
N_3=\{0,6,12,18\}.
\]

每个 arm 完整重优化：

\[
V_6
=
J(N_0)-J(N_1),
\]

\[
V_{12}
=
J(N_1)-J(N_2),
\]

\[
V_{18}
=
J(N_2)-J(N_3).
\]

不预设边际价值一定为正；若出现明显非单调，先查实现与参数同源性。

---

# 13. 灵敏度分析（简）

所有候选值在 January 预注册，正式期不得以 2–12 月最小费用反选主参数。

| 轴 | 主设置 | 对照 |
|---|---|---|
| price half-life | January 冻结 | 7 / 14 / 28 d |
| online correction | on | off |
| smearing hierarchy | issue×lead-block | issue-only |
| mean-consistency | on | raw-residual diagnostic |
| scaler | causal rolling | frozen-Jan |
| kernel bandwidth | frozen | 0.5 / 1 / 2 standardized units |
| ESS threshold | frozen | 0.15 / 0.25 raw ratio |
| leaves | ≤8 | 6 / 8 / 10 |
| tree granularity | stagewise 2-way max | coarse / main |
| tail | frozen | 3 / 5 / 7 d |
| Q4-2 value | P1 | P0 / P2 |
| Q4-3 uncertainty | joint tree | deterministic price |
| Q4-3 continuation | price-conditioned | PV-only |
| state | pooled SOC | regime-conditioned |
| MPC frequency | 10 min | 20 / 30 min |
| price visibility | causal | known-day-price |
| settlement | C | A，完整重优化 |
| efficiency | \(0.9/0.9\) | \(\sqrt{0.9}\) 语义反事实 |
| capacity / power | 题面 | ±10% |
| terminal | free | soft \(6000\) / common hard sensitivity |
| price lower bound | formal | LB-day / LB-horizon / LB-global |

统一报告：

\[
J_{cash},
\quad
J_{cmp},
\quad
S_{end},
\quad
H,
\]

\[
curtailment,
\quad
throughput,
\quad
PWL\ gap,
\]

\[
exactification\ gap,
\quad
\Delta_{policy},
\quad
runtime.
\]

---

# 14. 可选政策级 Pareto 扩展（不进入主调度）

若计算预算允许，可恢复完整的低维政策级 pymoo 扩展，但**不用于搜索 144 维购电轨迹**。

## 14.1 决策变量

\[
x=
(
H_p,
\alpha_{wd},
h_r,
K,
W_V,
K_{max},
\Delta_{MPC},
nowcast\_switch
).
\]

属于 mixed-variable problem。

## 14.2 三目标

\[
f_1(x)=J_{cmp}(x),
\]

\[
f_2(x)
=
CVaR_{0.95}
(
J_{emergency,d}(x)
),
\]

\[
f_3(x)
=
\sum_{d,t}
(C_{d,t}+D_{d,t}).
\]

第三目标若改弃光，必须在运行前固定，不能运行后挑好看的指标。

## 14.3 约束

统一：

\[
g_i(x)\le0.
\]

例如：

\[
g_1=fatal\_day\_count,
\]

\[
g_2=error\_count,
\]

\[
g_3=leakage\_fail\_count,
\]

\[
g_4=runtime-runtime_{budget},
\]

\[
g_5=ESS_{min}-ESS^{raw}_{P10}.
\]

fatal/error/leakage 必须为 0，不能靠大罚项掩盖。

## 14.4 算法与验证

- NSGA-II rank-and-crowding；
- pymoo mixed-variable interface；
- feasibility-first；
- multiple seeds；
- non-dominance audit；
- hypervolume convergence；
- extreme points；
- knee point；
- Pareto 候选最终由独立闭环求解器复算。

正文最多报告成本极端点、风险极端点和一个 knee point。

---

# 15. Result4 物化

## 15.1 result4-2

严格对应 result2。

### 表 1

指定 10 min 时段：

- \(G^{plan}\)；
- 全天计划购电量；
- 全天实际结算购电费。

### 表 2

六个 4 h 区间：

- 0:00–4:00；
- 4:00–8:00；
- 8:00–12:00；
- 12:00–16:00；
- 16:00–20:00；
- 20:00–24:00。

汇总实际：

\[
C^{exec},D^{exec}
\]

并给：

- 0:00 SOC；
- 24:00 SOC。

### 表 3

只列：

\[
H>0
\]

的连续紧急购电区间。同日相邻正槽合并，跨日不合并。

## 15.2 result4-3

严格对应 result3：

- \(G^0\)：0:00 plan；
- \(A^F\)：最终生效调整；
- \(A^6/A^{12}/A^{18}\)：仅审计长表；
- actual \(C/D/S\)；
- emergency interval；
- 读法 C + actual price 现金费用。

## 15.3 审计长表

至少保存：

```text
# authority / version
model_version
parent_hash
input_hash
config_hash
formal_q3_executor
legacy_q3_executor
qm_role
price_visibility

# time / availability
decision_time
available_at_max
train_start
train_end
shadow_chain_id

# price forecast
price_model_version
price_node_forecast
price_mpc_nowcast
smearing_group
smearing_sample_size
smearing_applied_count

# scaler
scale_version
scale_train_end
scale_median
scale_MAD
scale_floor

# scenario / tree
scenario_source_day
hard_compatible_count
scenario_raw_weight
ESS_raw
fallback_level
tree_stage
node_id
parent_node_id
leaf_id
leaf_to_node_map
node_leaf_count
scenario_medoid_weight
scenario_mean_price_gap
scenario_mean_PV_gap
stress_score

# continuation
regime_id
regime_sample_size
PWL_domain_min
PWL_domain_max
PWL_knots
PWL_values
PWL_duals
PWL_lower_coeffs
PWL_upper_coeffs
PWL_gap
PWL_role
active_knot_count
exact_knot_count
continuation_CD_violation
continuation_exactification_gap

# Q4-2 local value
lambda4
lambda4_dual
lambda4_fd_005
lambda4_fd_010
lambda4_fd_020
lambda4_iteration
compression_fallback
compression_regret

# Q4-3 contracts / SAA
A6
A12
A18
A_final
SAA_epigraph_z
LDR_B_matrix_norm
LDR_causality_max_violation

# MPC / recourse
MPC_terminal_node
MPC_PWL_version
MPC_horizon
MPC_reopt_flag
MPC_plan_age
MPC_ref
micro_mode

# result
S_start
S_end
J_cash
J_cmp
solver_status
milp_trigger_reason
milp_active_set_size
random_seed
```

---

# 16. 问题修复—验证台账

| ID | 问题 | 方案状态 | 验证状态 |
|---|---|---|---|
| P0-1 | flat analog paths 无 stage-node tree | specification-fixed | implementation-pending |
| P0-2 | double-smearing / scenario mean drift | specification-fixed | numeric-validation-pending |
| P0-3 | q/m、E1、MPC 继承冲突 | specification-fixed | regression-pending |
| P0-4 | continuation 未条件化 terminal regime | specification-fixed | January-gate-pending |
| P0-5 | price visibility 不是题面唯一事实 | specification-fixed | sensitivity-pending |
| P1-1 | compatible 概念模糊 | specification-fixed | implementation-pending |
| P1-2 | cold-start / shadow protocol 缺失 | specification-fixed | implementation-pending |
| P1-3 | January 多重调参 | specification-fixed | holdout-pending |
| P1-4 | Q4-2 frozen q/m 接口不闭合 | specification-fixed | Q2-replay-pending |
| P1-5 | MPC nowcast 原尺度回变换缺失 | specification-fixed | calibration-pending |
| P1-6 | scaler 生命周期不清 | specification-fixed | audit-pending |
| P1-7 | tail 未保持 L-PV-price 同源 | specification-fixed | correlation-pending |
| P1-8 | continuation “多数 knot”标准错误 | specification-fixed | exactness-pending |
| P1-9 | LDR/MILP closure 未矩阵化 | specification-fixed | small-instance-pending |
| P1-10 | LB-price 名称含糊 | specification-fixed | inequality-pending |
| P1-11 | state-sufficiency protocol 不闭合 | specification-fixed | January-gate-pending |
| P2-1 | stress score 过粗 | specification-fixed | representative-LP-pending |
| P2-2 | local FD 可能跨 kink | specification-fixed | multi-step-pending |
| P2-3 | fallback 被误称“稳定” | specification-fixed | wording-fixed |
| P2-4 | SAA source-flow 退化 | specification-fixed | lexicographic-pending |
| P2-5 | MPC 无 time-limit/fallback | specification-fixed | runtime-pending |
| P2-6 | 双版本号 | fixed：统一 v6.0 | verified |
| P2-7 | price 数据画像未写数值 | deliberate-pending | actual-data-pending |
| P2-8 | Gate 被误写成已验证 | fixed：四级状态 | verified |

---

# 17. 正式实施顺序

```text
P0  freeze authority/config/version hashes
P1  data/time/unit/positive-price Gate
P2  cold-start burn-in + shadow chain
P3  FORMAL_Q2 / FORMAL_Q3 regression harness
P4  rolling node price forecast + smearing
P5  MPC slot-level nowcast
P6  rolling-origin price residual
P7  PV revision/error replay
P8  raw mean-consistent paired paths
P9  hard compatibility + kernel + ESS_raw
P10 stagewise node tree + medoid reduction
P11 tail same-source-day block
P12 pooled/regime continuation
P13 active-knot exactness + adaptive PWL
P14 Q4-2 local Bellman oracle
P15 Q4-3 node-contract SAA + causal LDR
P16 SAA active-set MILP conflict closure
P17 MPC + two-mode recourse
P18 Jan rolling selection + holdout
P19 freeze all structural hashes before Feb
P20 Q4-2 334-day sequential formal run
P21 Q4-3 334-day sequential formal run
P22 independent validation
P23 result4-2 / result4-3 write + reverse readback
P24 pre-registered sensitivity / VOI
P25 optional policy-level Pareto appendix
```

---

# 18. Formal-run 写入条件

只有全部满足：

```text
static_semantics_gate          = PASS
data_gate                      = PASS
unit_gate                      = PASS
mapping_gate                   = PASS
price_visibility_gate          = PASS
positive_price_gate            = PASS
cold_start_gate                = PASS
formal_q2_regression           = PASS
formal_q3_regression           = PASS
scenario_mean_gate             = PASS
smearing_once_gate             = PASS
tree_nonanticipativity_gate    = PASS
ESS_raw_gate                   = PASS
tree_reduction_gate            = PASS
tail_joint_block_gate          = PASS
PWL_domain_gate                = PASS
active_knot_exactness_gate     = PASS
PWL_gap_gate                   = PASS
regime_gate_or_Kz1             = PASS
SAA_epigraph_gate              = PASS
LDR_causality_gate             = PASS
MILP_conflict_closure_gate     = PASS
MPC_terminal_gate              = PASS
physical_gate                  = PASS
settlement_gate                = PASS
solver_gate                    = PASS
parameter_hash_gate            = PASS
result_readback_gate           = PASS

fatal_day_count = 0
errors = []
```

才能把 Q4 数值写成“verified”。

---

# 19. 待计算结果

```text
# Q4-2
J_cash                           = 待计算
J_cmp                            = 待计算
S_end                            = 待计算
emergency_energy                 = 待计算
P1 local-value stability         = 待计算
P1-vs-P2 regret                  = 待计算

# Q4-3
J_cash                           = 待计算
J_cmp                            = 待计算
S_end                            = 待计算
emergency_energy                 = 待计算
adjustment_cost                  = 待计算
policy_gap                       = 待计算

# price / scenario
node price MAE/RMSE              = 待计算
MPC nowcast calibration          = 待计算
scenario mean gap                = 待计算
ESS_raw / fallback               = 待计算
tree node/leaf counts            = 待计算
tree reduction decision gap      = 待计算

# continuation
PWL max gap                      = 待计算
active-knot exactness rate       = 待计算
max LP-MILP exactification gap   = 待计算
pooled-vs-regime value           = 待计算

# information value
V6 / V12 / V18                   = 待计算
VOI_day_price                    = 待计算
LB_day/horizon/global_price      = 待计算
```

---

# 20. 最终方法总表

| 子问 | 数据预处理 | 建模 | 求解 | 核心验证 |
|---|---|---|---|---|
| Q4-2 | frozen Q2 PV + causal node price + rolling residual | \((q,m)\) frozen interface + daily LP + local Bellman marginal | HiGHS daily LP + local continuation oracle + transparent executor + active-set exactification | Q2 replay、future-price leakage、dual/FD、P1/P2 regret、physical、Jcmp |
| Q4-3 | official PV + mean-consistent price scenarios + tree-aware paired paths | node-contract conditional SAA + causal LDR + leaf/regime-consistent PWL + MPC + two-mode recourse | sparse HiGHS + stagewise tree reduction + parallel knot LP + conflict-closure MILP | tree nonanticipativity、smearing once、ESS_raw、PWL exactness/domain、LDR、MPC terminal、settlement、policy gap |

---

# 21. 最终竞争力定位

Q4 的高质量不来自“再加一个复杂算法”，而来自把新增的波动价格信息严格嵌入 Q2/Q3 原有决策权限，并把不确定性、状态价值和执行闭环做成可验证体系：

\[
\boxed{
\text{causal point forecast}
\rightarrow
\text{mean-consistent paired paths}
\rightarrow
\text{tree-aware compatible reduction}
\rightarrow
\text{node-contract conditional SAA}
\rightarrow
\text{leaf/regime-consistent continuation}
\rightarrow
\text{causal MPC}
\rightarrow
\text{two-mode physical recourse}
\rightarrow
\text{independent closed-loop validation}
}
\]

四问状态价值主线保持：

\[
\boxed{
Q1:\text{完整确定性 value}
\rightarrow
Q2:\text{低阶仿射 residual value}
\rightarrow
Q3:\text{PV-conditioned PWL}
\rightarrow
Q4:\text{PV-price-conditioned value}
}
\]

同时避免：

- hidden clairvoyance；
- future-price leakage；
- double-smearing；
- 场景均值漂移污染价值归因；
- q/m 与 SAA 双重保守；
- E1/MPC 版本混用；
- LP certificate 被误写成 MILP 真实 bound；
- 为省算力删除官方更新节点；
- 正式期事后调参；
- 旧粗略数字倒逼参数。

这使 Q4 从“方法很多”升级为“信息结构合法、数学闭合、求解可执行、结果可审计”的高竞争力方案。
