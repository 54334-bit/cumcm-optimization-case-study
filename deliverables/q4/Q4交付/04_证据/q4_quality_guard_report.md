# Q4 质量监理报告（q4_quality_guard，reviewer 只读）

> 生成：2026-09-12 ｜ 监理对象：`result4-2.xlsx`、`q4_data_anchor.json`、`q4_cp2_gate.json`、
> `q4_cp2_checkpoint.json`、`交付包_Q4-2_波动电价完整购电策略\交付清单.md`
> 依据：`8对话\Q4最终方案_v3_定稿.md` §5.1（G1/G2/G4/G5）
> 本报告只读产出：全部结论均为**我在本机重新读取原始 CSV / xlsx / JSON 后实测**，
> 未修改任何 xlsx / JSON / 代码（模板哈希见 §5）。

---

## 0. 一句话总判

**合格（限于 Q4-2）**：G1、G2、G4、G5 四项的**全部数值、哈希、结构判据通过**，独立校验器 `errors=[]`；
但 **Q4-3（`result4-3.xlsx`）尚未产出**，且 **G2 判据中的「375 段完全相等」没有任何落盘证据**——
这两条属交付/取证缺口，须由主对话确认后补（详见 §3）。

---

## 1. G1 数据锚点（逐条 + 我自己的复算）

复算口径：直接读 `D:\CMUCU\B对话\clean\*.csv`（逐格转 float），并额外回查官方原始附件。

| # | 判据（方案 §5.1 G1） | 实测（我的复算） | 锚点 JSON 值 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | 365×144 | load (365,144)、pv (365,144)、p4 (365,144) | 同 | ✅ |
| 2 | 无 NaN/inf | load 0/0、pv 0/0、p4 0/0 | 同 | ✅ |
| 3 | `#(p≤0)=0` | 0；p4 min = **0.0076**、max = **1.7936**、mean = **0.7661976046423136** | 0 / 0.0076 / 1.7936 / 0.7661976046423136 | ✅ 逐位一致 |
| 4 | 附件1 = 附件4 逐时段均值，max diff ≤ 1e-4 | **5.1506849315097e-05**（365 天全样本均值），corr = 0.9999999956086 | 5.150684931509719e-05 | ✅（但口径需注明窗口，见 §3-6） |
| 5 | 日期索引 2/1 ~ 12/31 | `days[31]=2025-02-01`、`days[364]=2025-12-31`、共 334 天 | 同 | ✅ |

**额外原始附件核对（我加做的，超出判据但必要）**——clean CSV 是否等于官方附件：

| 对照 | max abs diff | 结论 |
| --- | ---: | --- |
| `附件4.xlsx` vs `attachment4_clean.csv` | **0.0** | 逐格相同 |
| `附件1.xlsx` 电价/负荷/光伏 vs `q1_clean.csv`（144 行） | 0.0 / 0.0 / 0.0 | 逐格相同 |
| `附件2.xlsx`「小区负载」vs `attachment2_load.csv` | **0.0** | 逐格相同 |
| `附件2.xlsx`「光伏发电实际功率」vs `attachment2_pv.csv` | **0.0** | 逐格相同 |

> 即：整条链的输入不是「二手 clean」，而是与官方附件逐格一致。G1 **通过**。

---

## 2. G2 正确性闸门（附件1 价格复现 Q2）

| 判据 | 要求 | 实测 | 差值 | 容差 | 结论 |
| --- | --- | ---: | ---: | ---: | --- |
| 全年费用 | 13,252,341.09 ± 0.01 | **13,252,341.092092** | +0.002092 | 0.01 | ✅ |
| `J_plan` | 差 ≤ 0.01 | **12,891,818.24484** | +0.00484 | 0.01 | ✅ |
| `J_emg` | 差 ≤ 0.01 | **360,522.847252** | −0.002748 | 0.01 | ✅ |
| 紧急 242 天 | 完全相等 | **242** | 0 | 0 | ✅ |
| 紧急 375 段 | 完全相等 | **未落盘、无法核验** | — | — | ⚠️ 见 §3-2 |
| 年末 SOC | 7,950.0 | **7,950.00009** | +9e-05 | 0.05 | ✅ |
| `QH`（我补核） | Q2 公布 55,801.05 | **55,801.045903** | −0.004097 | （判据误写 0.05） | ✅ 数值相符 |

闸门运行的 ①~⑧ 防退化断言：全部 `pass=true`（最差能量残差 9.663e-13、SOC 递推残差 0.0、
最差负流量 0.0、`max(R_PV−PV·dt)=0.0`、χ=0、`Q_RPV/Q_PV=0.1108`）。**闸门整体 `pass=true`，未触发"不合格"条款。**

---

## 3. 不一致清单

### 严重问题

1. **`result4-3.xlsx` 整份缺失。** 全仓检索（`8对话`、`7.5对话`、交付包、`赛题\C题\附件\附件5`）只存在
   官方**空白模板** `result4-3.xlsx`（265,429 B，mtime 2026-09-10 18:40:22），**没有任何 Q4-3 成品**；
   `7.5对话\output` 也未见 `result3.xlsx` 成品（只有 `q75_*` / `g2_runs\*` 探针 JSON）。
   差值 = 1 份提交件（约 4 张表）完全缺失。
   → 与方案 §8 fallback 并不冲突（Q4-3 依赖 Q3 v3，Q3 未交付），**但交付包内没有任何"Q4-3 待接"的文字声明**，
   收件方会误以为 Q4 已完整交付。**要求：在交付说明/清单中显式写出"Q4-3 待 Q3 交付后补跑，本包不含"。**

2. **G2 判据中的「紧急 375 段完全相等」无任何落盘证据。** `q4_cp2_gate.json` 的 `items` 只有
   total / J_plan / J_emg / emg_days / QH_kWh / s_end 六项，**没有段数项**；Q2 侧的 375 段来自
   `5对话\交付包_Q2_因果裕度非预见\evidence\q2_emg_detail.json`（`emergency_segments: 375`）。
   间接旁证（非等价）：段数与 H 数组同源，QH 已到 −0.0041 kWh、天数 242 完全相等。
   → 要求：`make_gate()` 增加 `emg_segments` 项（`H>1e-9` 同日相邻合并），或把闸门 H 数组落盘后复核；
   闸门重跑本身仅约 7.5 s，成本可忽略。

### 需要改进

3. **G4「汇总量与闸门一致」的措辞会误导。** 闸门跑的是**附件1**价格（13,252,341.09），
   主 checkpoint 跑的是**附件4**价格（13,850,454.64），二者**本来就不应相等**，差值
   **+598,113.55 元 = +4.513%**（与方案 §8「+4.51%」表述一致）。核查点应为
   「checkpoint 内部自洽 + 与闸门共用同一代码链」，而 checkpoint 本身没有价格标签以外的闸门数值可对。
   要求：把 G4 判据改写为"逐日求和 = annual 汇总量"，删去"与闸门一致"的歧义表述。

4. **闸门 `QH_kWh` 项量化不当。** `expected` 写成整数 **55,801.0**、`tol=0.05`；
   Q2 公布值是 **55,801.05**。这等于把有效容差放宽到 ±0.05 kWh，本次实测 ±0.0041 通过，
   但判据本身松了 10 倍且期望值取整错误。要求：`expected=55801.05`、`tol=0.01`。

5. **G1 第 4 条未注明均值窗口，按字面可有 300 倍差异。** 我实测：
   - 365 天全样本均值 vs 附件1：max diff **5.1507e-05** ✅（锚点采用）
   - 评估窗 334 天（d31..d364）均值：**0.0306467066** ❌（超判据 306 倍）
   - 仅 1 月 31 天均值：**0.3306451613** ❌
   要求：在 `q4_data_anchor.json` / 交付说明中把判据写成"**附件4 全 365 天**逐时段算术均值（四舍五入到 4 位）"，
   否则第三方会按评估窗复算并判失败。

6. **交付包内容与方案 §9 承诺不符。** 实存 12 个文件；方案 §9 承诺的
   `evidence\q4_review_independent.md`（独立复算报告）、`q4_probe_price_caliber_v2.json`、
   `q4_probe_lambda_joint.json` 三项**缺失**；`code\` 声明"9 个脚本快照"，实存 **4 个**
   （q4_data_io / q4_det_seq / q4_materialize / q4_validator）。差值 = 3 文件 + 5 脚本。
   （另：任务书写的 `交付包_Q4\` 实际为 `交付包_Q4-2_波动电价完整购电策略\`，路径名不一致。）

7. **交付说明内 `QH` 有两个数字未标注关系。** 关键数字表写 **57,204.245183 kWh**（checkpoint），
   §2 末尾写 **57,204.245176 kWh**（表3 逐段 6 位舍入后求和），差 **7e-6 kWh**。
   要求：二选一为主值并加一句"表3 合计因逐段 6 位舍入差 7e-6 kWh"。

### 优化建议

8. 建议在闸门/主线 JSON 顶部各加 `"caliber_price": "attachment1" | "attachment4"` 标签，
   避免后续复用方把 13,252,341.09 与 13,850,454.64 误判为"同一实验不一致"。
9. `交付清单.md` 中 sha256 大小写不统一（`dae72568…` 小写 / `7176722c…` 小写，而 §5 记录值大写），
   建议统一小写；清单已声明"不含本文件自身"，此点无问题。
10. 表1 第 147 列模板表头为「全天购电费」，实际只含 `Σ p4·G`（不含紧急购电 5p·H）。
    交付说明已声明，建议在交付清单里也复述一次（评审常只读清单与表）。

---

## 4. G4 主跑（334 天）

| 项 | 实测 | 结论 |
| --- | --- | --- |
| 天数 | **334**，`d = 31..364` 连续、无重复、无缺 | ✅ |
| 逐日字段完整性 | `G/C/D/H/R_PV/R_G` 均 144 段；`S0/S1/J_plan/J_emg/QG/QH/margin_d/bal_resid_max` 无缺键；NaN/inf = 0 | ✅ |
| 逐日求和 vs annual | `J_plan` −0.000007、`J_emg` −0.000001、`QG` +0.000005、`QH` +0.000007（逐日 6 位舍入所致） | ✅ |
| 年度总额 | 13,850,454.638123 = 13,473,614.405396 + 376,840.232727 | ✅ |
| 紧急 | 238 天（逐日 `QH>1e-9` 计数 = `annual.n_emg_days` = 238）；同日相邻合并 = **398 段** | ✅ 与物化报告一致 |
| SOC | 全时段 min 1200.0 / max 10800.0；年末 **7,200.00009** | ✅ |
| 逐日平衡残差 | 最差 1e-12 ≤ 1e-6 | ✅ |
| 独立性抽查 | 与 Q2（附件1）逐日对比：`ΣG` 平均差 2,229.03 kWh/天、最大 9,459.28 kWh；H 向量完全相同的 89 天**全部是零紧急日** | ✅ 无"照抄 Q2"迹象 |

---

## 5. G5 物化（`result4-2.xlsx` vs 官方模板）

模板：`D:\CMUCU\赛题\C题\附件\附件5\result4-2.xlsx`（只读）。

| 检查 | 判据 | 实测 | 结论 |
| --- | --- | --- | --- |
| ① 表数与形状 | 同模板 | 3 表同名同序（计划购电量 / 充放电量 / 紧急购电量）；表1 **335×147**（=模板）、表2 2005×6（模板 20×6 为样例）、表3 399×3（模板 11×3 为样例） | ✅ |
| ② 列非空 | 146/147 | 表1 全部 **147** 列均有数据：A 列为 334 个日期，**B..EQ 共 146 个数值列全部非空**，无任一空列 | ✅ |
| ③ 位置映射 `col=2+t` | 抽 3 天，第2列 = checkpoint `G[0]` | 抽 4 天（2025-02-01 / 05-12 / 08-20 / 12-31）：B = `G[0]` diff **0.00e+00**；col145 = `G[143]` diff **0.00e+00**；col146 = `ΣG` diff **0**；col147 = `Σp4·G` | ✅ |
| ④ 紧急表日期只写一次 | 当日首段 | 398 段中日期单元格 **238** 个、对应 **238** 天、**连续重复 0**、逐段时刻/电量无空值；表3 合计 57,204.245176 | ✅ |
| ⑤ 模板未被覆盖 | 哈希 | 当前 `1C26494CFC6D754E0BD9BFF7E13E1126A73D2D2DA6C5336EB251D89B9A1A1A47` **= 物化时记录值**；mtime **2026-09-10 18:40:22**（早于 9/12 19:33 物化）；模板表1 数据区仍全空 | ✅ |

补充核验（我加做）：表2 六个 4 小时块的 C/D 合计与 checkpoint 逐段求和**逐块相同**（差 ≤1e-8）；
「时刻/储电量」只写在每天前两行（668 = 334×2 个单元格，位置规则 100% 符合）；
年末 SOC 7,200.00009 已写入表2 末行；`output\result4-2.xlsx` 与交付包内副本哈希一致
（`4F63FD2833BC4C55BF04BADD04AF3D6B8B09F7FBFBB6A18CA4927C1A53719455`）。

> 附注：官方 `附件5\result4-2.xlsx` 与 `result2.xlsx` **字节相同**（同为 1C26494C…），
> `result4-3.xlsx` 与 `result3.xlsx` 字节相同（C59DA470…）——即模板本身是同一份，Q4-2/Q4-3 的差异只在填充数据。

---

## 6. 哈希清单核对（`交付包_Q4-2_波动电价完整购电策略\交付清单.md`）

逐条重算 sha256 与字节数：**11/11 完全一致，0 处不符**。

| 文件 | 字节 | sha256 是否一致 |
| --- | ---: | --- |
| `evidence/code/q4_data_io.py` | 5,988 | ✅ |
| `evidence/code/q4_det_seq.py` | 21,036 | ✅ |
| `evidence/code/q4_materialize.py` | 7,594 | ✅ |
| `evidence/code/q4_validator.py` | 26,731 | ✅ |
| `evidence/q4_cp2_checkpoint.json` | 2,393,711 | ✅ |
| `evidence/q4_cp2_gate.json` | 3,621 | ✅ |
| `evidence/q4_cp4_materialize.json` | 945 | ✅ |
| `evidence/q4_data_anchor.json` | 2,722 | ✅ |
| `evidence/q4_validator_result.json` | 6,760 | ✅ |
| `result4-2.xlsx` | 426,047 | ✅ |
| `交付说明_Q4-2_波动电价完整购电策略.md` | 6,095 | ✅ |

包内实际文件 12 个 = 上述 11 个 + `交付清单.md` 自身（清单已声明不含自身）。
包内 `evidence/q4_cp2_checkpoint.json` 与 `8对话\output\` 原件哈希相同（D84DA2B0…），
`result4-2.xlsx` 双份哈希相同（4F63FD28…）——**交付包与工作产物同源，无二次改动**。

独立校验器（G6 旁证）：`q4_validator_result.json` 共 11 项检查全 `pass`，`errors = []`；
`q4_validator.py` 仅 import `json/os/datetime/numpy/openpyxl`，**未 import 任何 `q4_*` 求解代码**，
符合"校验器不得依赖求解器"的要求。

---

## 7. 复核边界声明

- 我**没有重跑** `q4_det_seq.py`（会覆盖 checkpoint / gate JSON，越权且破坏产物）；G2/G4 的核验方式是
  **读产物 + 交叉对账 + 与 Q2 交付证据逐日比对**，不是二次求解。
- 未修改任何 xlsx / JSON / 代码；本报告是本次任务唯一写入。

## 8. 建议主对话动作（按优先级）

1. 在交付说明与清单中补「Q4-3 待 Q3 交付后补跑，本包不含」声明（否则属交付误导）。
2. 让 `make_gate()` 补 `emg_segments`（期望 375）并重跑闸门一次；同时把 `QH` 期望改为 55,801.05 / tol 0.01。
3. 在 `q4_data_anchor.json` 注明均值窗口为「附件4 全 365 天」。
4. 决策是否补齐方案 §9 承诺的 3 个 evidence 文件与 9 个脚本快照；不补则在清单里删除该承诺。
