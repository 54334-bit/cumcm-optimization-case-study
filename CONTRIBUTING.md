# 贡献指南

欢迎改进原创代码、文档、测试和复现说明。提交前请确认：

1. 不提交题面、官方附件、官方模板、竞赛或 AI 规定 PDF、第三方论文、字体、二进制、缓存和压缩交付包。团队自行授权发布的交付工作簿可提交至 `submissions/`，但必须同时更新其 SHA-256、来源说明和验证记录。
2. 不提交令牌、密码、个人绝对路径、个人配置或未脱敏日志。
3. 模型变更必须区分题面事实、建模假设、源结果与新验证结果；不得把历史记录改写成当前复现结论。
4. 不得把 Q2 与 Q3 的不同信息结构结果做简单优劣差额解释。
5. 为新增行为先添加测试，并运行 `python -m pytest -q tests` 与 `python -m compileall -q src scripts tests`。

涉及数据、图件、论文模板或第三方资料时，请先在变更说明中写明来源、许可证、再分发权限和替代方案。证据不足时仅提交元数据、哈希和获取说明。
